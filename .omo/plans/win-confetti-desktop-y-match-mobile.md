# Brief: 桌面胜利彩纸发射高度对齐移动端

用户反馈桌面彩纸从底部发射不如移动端好看，希望发射点不用贴底，改到和移动端
一样、稍高的位置。按用户的明确对照项，本次将桌面 `origin.y` 从 `0.96` 改为
移动端现有的 `0.55`。桌面 `origin.x=0.18/0.82` 继续保留，移动端
`x=0/1`、`y=0.55` 不变；角度、粒子数、节奏、调色板和 reduced-motion 行为
全部冻结。

验证以先改后红的桌面单元断言锁定 `y=0.55`，再用生产构建浏览器探针检查两个
视口的 mid-height 发射带与中央 UI 可点击性。
