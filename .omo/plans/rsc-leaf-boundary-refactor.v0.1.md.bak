# Plan: RSC Leaf Boundary Refactor — DRAFT

> **Status: DRAFT (v0.1, 2026-09-11)**
> 草稿阶段。未经 momus 复审、未与用户拍板。`Plan:` footer 待定。
> 草稿目标：把上一轮 RSC vs Client Components 调查（参考 [AGENTS.md §概览](/private/tmp/tic-tac-toe/AGENTS.md)）的 5 条建议落成可执行的实施步骤与验收条件，等用户决策后再升级为 MOMUS-reviewed 终稿。

---

## TL;DR（For humans）

**What you'll get**：3 个游戏页面（`/`、`/play`、`/result`）从 `'use client'` 改成 RSC；`useRouter` / `useEffect` / `useGameStore` 这三个 client-only 入口下沉到叶子 client 组件；`SoundToggle` 上移到 `app/layout.tsx`；`stats` 数据从 client fetch `/api/stats` 改为 RSC 直读 `lib/db.ts`。

**Why this approach**：项目当前 client bundle 约 668K（12 chunks 未压缩），其中 `useRouter` 把 160K 的 Next router glue 拖进首屏，业务代码本身其实很轻。把 page 改回 RSC 后，所有展示型基础组件（`ui/Card`、`Button`、`StatsGrid`、`StatusBar`）依然以 RSC 形态被 server 渲染（React/Next 官方明确推荐「specific interactive components should be client, large parts stay server」）；交互逻辑收敛到叶子（`Board`、`Cell`、`SoundToggle`、`Confetti`、`StartGameButton`、`PlayController`、`ResultActions`），client graph 收缩。

**What it will NOT do**：不改 `lib/game.ts` 纯规则、不改 `lib/db.ts` 行为、不改 `DESIGN.md`/视觉/可达性契约、不删 `/api/stats` 路由（保留作为 PUT/DELETE 入口）、不引入新 npm 依赖、不动 Tailwind tokens、不改 commit-msg hook。

**Effort**：Medium
**Risk**：Low–Medium。client boundary 改写属于「refactor crossing domain boundaries」（[ultrawork Tier triage](/Users/onepisya/.codex/plugins/cache/sisyphuslabs/omo/4.19.4/skills/ultrawork/SKILL.md) 的 HEAVY 信号之一），但本仓库 6 层 Gauntlet 完备、改动可分原子提交并独立验证，所以实际 HEAVY 化只发生在「跨模块同步改动时」，单 commit 内仍是 LIGHT。

> TL;DR (machine): Medium / Low-Medium. 改动文件 ⊆ {`app/page.tsx`, `app/play/page.tsx`, `app/result/page.tsx`, `app/layout.tsx`, `components/SoundToggle.tsx`, `components/StartGameButton.tsx`(new), `components/PlayController.tsx`(new), `components/ResultActions.tsx`(new), `lib/store.ts`(可选), `tests/qa/*.mjs`(可选扩展)}. 零新 npm 依赖。


---

## Background & Evidence

### 1. 官方依据（React 19 + Next.js 16）

- [react.dev/reference/rsc/use-client](https://react.dev/reference/rsc/use-client)
  - "Server Components cannot support interaction as event handlers must be registered and triggered by a client."
  - "Server Components cannot use most Hooks."
  - "Prop values passed from a Server Component to Client Component must be serializable."
- [nextjs.org/docs/app/guides/server-and-client-boundary](https://nextjs.org/docs/app/guides/server-and-client-boundary)
  - **"You only need `'use client'` at the entry to a client subtree, not on every file inside it. Every module imported from that entry becomes part of the client module graph."**
  - "Passing rendered output as `children` lets a Server Component nest inside a Client Component without importing the Server Component's code into the client graph."
- [nextjs.org/docs/app/getting-started/server-and-client-components#reducing-js-bundle-size](https://nextjs.org/docs/app/getting-started/server-and-client-components#reducing-js-bundle-size)
  - **"To reduce the size of your client JavaScript bundles, add `'use client'` to specific interactive components instead of marking large parts of your UI as Client Components."**
- [nextjs.org/docs/app/api-reference/directives/use-client](https://nextjs.org/docs/app/api-reference/directives/use-client)
  - "You do not need to add the `'use client'` directive to every file that contains Client Components. You only need to add it to the files whose components you want to render directly within Server Components."

### 2. 项目本地约束

- `components/AGENTS.md`：**"只有交互组件使用客户端模式；展示型基础组件保持 server-compatible。"** 本 plan 与该既有约束一致。
- `AGENTS.md` §验证门禁：6 层 Gauntlet（Tests / Types / Lint / Build / Commit-audit / Browser QA）+ on-demand（Coverage / Mutation / Property-based）。所有改动必须经此门禁。
- `AGENTS.md` §提交约定：原子 commit，每份独立 build + test 为绿；非平凡提交 footer `Plan: .omo/plans/<slug>.md`。

### 3. 当前状态（实测）

- 3 个 page 文件全部 `'use client'`：`app/page.tsx:1`、`app/play/page.tsx:1`、`app/result/page.tsx:1`。
- `app/play/page.tsx` 是唯一含 `useEffect` 的页面（3 处），用于：①auto-start game；②setTimeout → `router.replace('/result')`；③cleanup。
- `app/page.tsx` / `app/result/page.tsx` 仅通过 `useGameStore((s) => s.xxx)` 订阅状态 + `onClick` 调用 store action → 严格来说不需要整页 client，只需订阅层是 client。
- 业务 client 组件：`Board` / `Cell` / `SoundToggle` / `Confetti` / `ServiceWorkerRegister` —— 全部正确标记。
- `.next/static/chunks/` 实测：12 个 chunk / 668K 未压缩。其中 `3ta1i4aoxr3of.js` 160K 含 `dispatchAppRouterAction` —— 由 `useRouter` 拖入。
- `lib/db.ts` 已导出 `loadStats()` / `saveStats()` / `resetStats()` —— RSC 可直接 `await loadStats()`，无需新增 API。


---

## Scope

### Must have（按 ROI 排序，每条至少一份原子 commit）

1. **`components/SoundToggle.tsx` 上移到 `app/layout.tsx`**（小，最先做，零行为变化）
   - 在 `layout.tsx` `<body>` 内固定位置挂一次（建议 absolute top-right）；所有页面自然共享。
   - 删除 home / play / result 三处 import 和 JSX。
   - Acceptance：3 个页面仍可点击 sound-toggle 切换；音效行为不变；`tests/qa/audio-probe.mjs` 仍 PASS。

2. **`app/page.tsx` 从 client 改 RSC，提取 `<StartGameButton>`（client）**
   - Page 改成 `async`，内部 `const stats = await loadStats()` 直接 RSC 拿数据。
   - `useGameStore` 的 `stats` 不再由 page 订阅，改由 RSC 拿到的 stats 通过 props 传给 `<StatsGrid>`。
   - `<StartGameButton>` 是新的 client 组件：内部 `const startGame = useGameStore((s) => s.startGame); return <Link href="/play" onClick={startGame}><Button .../></Link>`。
   - 重置战绩按钮同样抽成 `<ResetStatsButton>`（client）。
   - Acceptance：`/` 首屏 HTML 含 `<StatsGrid>` 数据（之前需 hydration 后才出）；`pnpm build` 后 `node tests/qa/visual-qa.mjs` snapshot 中 `stat-value` 字段不变；`tests/qa/hydration-check.mjs` 仍 PASS（无 SSR mismatch）。

3. **`app/play/page.tsx` 从 client 改 RSC，提取 `<PlayController>`（client）+ 保留 `<Board>`**
   - Page 改成 `async` RSC（不需要 await 数据，因为 play 页主要靠 client 状态）。
   - `<PlayController>` 接管 `useEffect` auto-start + `router.replace('/result')` 跳转；用 `<PlayController>` 包裹 `<Board>`。
   - Acceptance：`/play` 首次访问 auto-start 不变；胜利/平局 700ms 后跳 `/result` 不变；`tests/qa/win-flow.mjs` 仍 PASS；`tests/qa/keyboard-roving.mjs` 仍 PASS。

4. **`app/result/page.tsx` 从 client 改 RSC，提取 `<ResultActions>`（client）**
   - Page 改成 `async`，`const stats = await loadStats()`。
   - `<ResultActions>` 接管"再来一局 / 返回首页 / 重置战绩"三个 client 动作（含 `startGame`、`restart`、`resetAll`）。
   - `<Confetti />` 保留 client（已经在 client）；父 page 是 RSC → Confetti 的代码依然进 client graph（无变化）。
   - Acceptance：`/result` 首屏 HTML 含战绩；`tests/qa/confetti-probe.mjs` 仍 PASS；`tests/qa/hydration-check.mjs` 仍 PASS。

5. **可选（推荐，但需用户决策）：`lib/store.ts` 把 `stats` 从 store 移到 RSC-only**
   - 现状：`useGameStore` 同时管 `board` / `phase` / `stats`；stats 由 hydrateStats → `/api/stats` 拿。
   - 目标：`board` / `phase` 仍在 store；`stats` 由 RSC `await loadStats()` 拿，通过 props 传给展示组件。
   - **API 契约**：`useGameStore` 的 `stats` 字段保留（read-only 副本，用于 store 内部 action 写回时的中间状态），但展示组件不再直接订阅；展示路径走 RSC → props → `<StatsGrid>`；写入路径（resetAll → saveStats → 路由刷新）保留 PUT/DELETE 行为。
   - 收益：首屏 HTML 含 stats（更早可见）、hydration 后无 fetch、no flicker。
   - 风险：`/result` 硬刷新看到的战绩现在来自 RSC（之前来自 client `/api/stats` GET），如果 RSC 失败 → 页面降级到空 stats（之前是 client 失败后空 stats，行为一致）。
   - Acceptance：所有 store 单测通过；Stryker mutation on `lib/store.ts` 通过（详见 verification）。

### Must NOT have（scope guardrails）

- 不引入新 npm 依赖（`server-only` / `client-only` 暂不引入 —— 见 Open Question 4）。
- 不修改 `lib/game.ts`（Stryker target，行为不变）。
- 不修改 `lib/db.ts`（仅在 RSC 中 `import { loadStats }`，不改语义）。
- 不修改 `db/schema.ts`。
- 不修改 `app/api/stats/route.ts`（保留 PUT/DELETE 入口 + Node runtime 选择）。
- 不修改 `components/Board.tsx` / `Cell.tsx` / `Confetti.tsx` / `ServiceWorkerRegister.tsx`（已正确）。
- 不修改 `components/ui/Card.tsx` / `Button.tsx` / `StatsCard.tsx` / `StatsGrid.tsx` / `StatusBar.tsx`（已正确保持 RSC）。
- 不修改 `DESIGN.md` / `app/globals.css`（视觉/可达性契约）。
- 不修改 Tailwind class、不引入新色板或内联 hex。
- 不删任何现有 data-testid（QA 契约）。
- 不修改 `AGENTS.md` / `CONTRIBUTING.md` / `docs/verification-gauntlet.md`。
- 不使用 `git commit --no-verify`。


---

## Verification strategy

引用项目 6 层 Gauntlet（[docs/verification-gauntlet.md](/private/tmp/tic-tac-toe/docs/verification-gauntlet.md)）：

| 层 | 工具 | 触发 | 产物路径 |
|---|---|---|---|
| Tests | vitest 5 + jsdom 30（88 例） | ✅ 每 commit | `pnpm vitest run` 退出 0 |
| Types | tsc 5 strict | ✅ 每 commit | `pnpm typecheck` 退出 0 |
| Lint | eslint 9（tests/qa/\*\* ignore） | ✅ 每 commit | `pnpm lint` 退出 0 |
| Build | next build | ✅ 每 commit | `pnpm build` 退出 0；route 数不变 |
| Commit-audit | `node tests/qa/commit-audit.mjs` | ✅ 每 commit | 0 violations |
| Browser QA | `tests/qa/*.mjs` | ✅ 每 commit（改动触及 UI） | 至少：`visual-qa` / `hydration-check` / `audio-probe` / `audio-confetti-qa` / `win-flow` / `keyboard-roving` / `confetti-probe` 全部 PASS |
| Coverage | vitest --coverage（lib/\*\* + db/\*\*） | ⚠️ on-demand | scope 命中（`lib/store.ts` 改动触发） |
| Mutation | Stryker 4 files scope | ⚠️ on-demand | scope 命中（`lib/store.ts` 改动触发） |
| Property-based | fast-check | ⚠️ on-demand | 不新增 lib/X.ts 纯函数 → 不触发 |

附加手动验收（human-in-loop）：
- `pnpm start` 生产构建启动后，浏览器实测 3 个页面首屏 HTML 中战绩可见（用 `view-source:` 看源码）。
- `chrome-devtools-axi` Performance：Largest Contentful Paint 不退化（基线：现有 LCP < 1.5s on Vercel prod）。

附加 evidence 产物：
- `task-{1..N}-build.log`
- `task-{1..N}-vitest.log`
- `task-{1..N}-typecheck.log`
- `task-{1..N}-lint.log`
- `task-{1..N}-commit-audit.log`
- `bundle-comparison.md`：改动前后 `.next/static/chunks/` 总大小对比（diff 表）

---

## Execution strategy

### Atomic commits 拆分（建议顺序）

按 ROI 升序，每份独立 build + test + commit。

| # | Commit 主题（占位，最终以 review 后定） | 改动文件 | 风险 |
|---|---|---|---|
| C1 | `refactor(layout): hoist SoundToggle to root layout` | `app/layout.tsx`、3 个 page 文件（删除 import） | 极低 |
| C2 | `refactor(home): convert home page to RSC + extract StartGameButton` | `app/page.tsx`、新增 `components/StartGameButton.tsx`、新增 `components/ResetStatsButton.tsx`（可选） | 低 |
| C3 | `refactor(play): convert play page to RSC + extract PlayController` | `app/play/page.tsx`、新增 `components/PlayController.tsx` | 中（useEffect 顺序保持需论证） |
| C4 | `refactor(result): convert result page to RSC + extract ResultActions` | `app/result/page.tsx`、新增 `components/ResultActions.tsx` | 低 |
| C5（可选） | `refactor(store): decouple stats from zustand; RSC owns stats fetch` | `lib/store.ts`、3 个 page 文件、`<StatsGrid>` 调用方 | 中–高 |

### Dependency matrix

| Commit | Depends on | Blocks | Can parallelize with |
|---|---|---|---|
| C1 | — | C2, C3, C4 | （独立前置） |
| C2 | C1 | — | C3, C4（文件不冲突） |
| C3 | C1 | — | C2, C4（文件不冲突） |
| C4 | C1 | — | C2, C3（文件不冲突） |
| C5 | C2, C3, C4 | （最终） | （独立最后做，跨多文件） |

C2 / C3 / C4 互不冲突，理论上可并行；但为减少 git 冲突风险和评审负担，建议串行（每份独立 review + merge）。

### Optional: Property-based / Mutation / Coverage

仅 C5 改动 `lib/store.ts` 时触发 on-demand 层：先跑 coverage 看 80/80/70/80 阈值，再跑 Stryker mutation 看 84.50% 基线不退化。


---

## Todos

> Implementation + Test = ONE todo. 草稿阶段先列出，后续随用户决策细化。

- [ ] **D1. 与用户对齐 open questions**（见下） → 草稿 v0.2
- [ ] **D2. C1: hoisting SoundToggle**
  - 改 `app/layout.tsx` 在 `<body>` 顶部 absolute 挂一次
  - 删 3 个 page 文件的 import + JSX
  - verify: 6 层 Gauntlet + audio-probe
- [ ] **D3. C2: home page → RSC**
  - 写 `<StartGameButton>` client 组件
  - 改 `app/page.tsx` 为 async + `await loadStats()`
  - verify: 6 层 Gauntlet + visual-qa + hydration-check
- [ ] **D4. C3: play page → RSC**
  - 写 `<PlayController>` client 组件（接管 useEffect + useRouter）
  - 改 `app/play/page.tsx` 为 RSC
  - verify: 6 层 Gauntlet + win-flow + keyboard-roving
- [ ] **D5. C4: result page → RSC**
  - 写 `<ResultActions>` client 组件
  - 改 `app/result/page.tsx` 为 async + `await loadStats()`
  - verify: 6 层 Gauntlet + visual-qa + hydration-check + confetti-probe
- [ ] **D6（可选）: C5: 解耦 store.stats**
  - 保留 board/phase 在 store；stats 走 RSC + props
  - 删 `useGameStore.hydrateStats` 或转为 no-op
  - verify: 6 层 Gauntlet + Stryker mutation + coverage
- [ ] **D7: bundle 对比**
  - 改前 `.next/static/chunks/` 总大小 vs 改后
  - 输出 `bundle-comparison.md` 到 `.omx/evidence/rsc-leaf-boundary-refactor/`

---

## Open Questions（需要用户决策后才能定稿）

> 这些是我做不了的判断，等用户回复后再写 v0.2。

1. **C5（store.stats 解耦）做不做？**
   - 收益：首屏 HTML 含战绩、无 client fetch flicker；但跨多文件改动，risk 升一档。
   - 不做：仍保留 `useGameStore` 的 `stats` 字段做展示订阅，client 端 hydrate；首屏仍空，hydration 后填上。
   - 默认推荐：**做**。这是把 RSC 优势发挥到位的关键一步。

2. **`SoundToggle` 在 layout 的位置？**
   - 现有：每个 page 右上角，与 page header 同列。
   - 上移后：layout 顶部 right-aligned absolute；不影响页面布局。
   - 是否保留 "右上角 + 与 page header 同列" 视觉？若保留，每个 page 仍需在 page 留 slot → 反而增加 page 复杂度。建议改为 layout top-right fixed。

3. **Page 改 RSC 后如何处理 `<Confetti />`（仍在 client）？**
   - 选项 A：保留 `app/result/page.tsx`（RSC）里 `<Confetti />` 直挂在 result headline 下 → Confetti 代码仍进 client graph（与现状一致），但 page 本身不触发 client runtime（OK）。
   - 选项 B：把 Confetti 改成条件挂载 client 组件，根据 RSC 拿到的 `lastOutcome` 决定是否渲染 → 可减一次 hydration 成本（如果 won，则加载 Confetti 模块；否则不加载）。代价是 conditional require 的边界要小心。
   - 默认推荐：A（简单 + 与现状一致）。

4. **是否引入 `server-only` / `client-only` 包？**
   - 官方推荐用于防"环境中毒"。本项目 RSC 直读 `lib/db.ts` 后，如果有人不小心在 client 文件 `import { loadStats }`，构建期会报错（因为 `@libsql/client` 用 `node:fs`）。
   - 引入 `server-only` 是 1 行 import 改 + 1 个新 dep；不引入则靠约定 + ESLint。
   - 默认推荐：**不引入**（项目反模式规则已经禁止引入新依赖）。

5. **是否同步升级 `next` / `@libsql/client` 等？**
   - 答：不要。`engines.node = 24.x` 与 vite-plus shim 解析对齐到 24.21.0 是已有契约（`AGENTS.md` §验证门禁有详细说明），不能动。

6. **是否需要写 Plan footer 之外的额外文件？**
   - 例如：是否更新 `docs/learnings.md` 加一条 "RSC leaf boundary refactor"？
   - 默认推荐：是，加一条；不超 30 行。

7. **是否拆 PR / 分支？**
   - 默认：单分支 `refactor/rsc-leaf-boundary`，C1–C5 共 4–5 commit；非紧急情况不拆 PR。
   - 若用户希望 PR-review 节奏更细，可拆 5 个 PR。

8. **是否需要同步更新 `docs/verification-gauntlet.md`？**
   - 不需要。本 plan 完全沿用既有 Gauntlet。
   - 唯一例外：如果 C5 决定做，需要在 docs 中新增 `lib/store.ts` mutation target 标注。

---

## Decision Log（草稿期间记录用）

- **DRAFT v0.1（2026-09-11）**：初始草稿。上一轮调查结论 → 5 条建议 → 4 份原子 commit + 1 份可选。8 个 open question 待用户回复。
