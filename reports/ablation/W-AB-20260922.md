# W-AB — ulw-quality-hardening-opt-20260922 双对象消融

**工单：** W-AB
**日期：** 2026-09-22 (Asia/Shanghai)
**席位：** tic-tac-toe 项目科学席
**Worktree：** `/Users/onepisya/.hermes/skills/job-hunter/portfolio/ttt-wab`
**分支：** `ulw/wab-ablation`
**基线 tag：** `dev-stable-20260922` = `e43f58f`（brief 中"6c1a972"为基线前最近 docs commit，与本工单无逻辑关联）
**HEAD：** `88ee15c`（refactor game-net httpJson）
**QA 起服：** `PORT=3104` + `DATABASE_URL=file:/tmp/ulw-wab/wab-<uuid>.db` + `pnpm build && pnpm start`

---

## 0. Teach-back（自述复述）

### 对象 (a) 本波优化变更因果对比 — 三项
1. **D-1 字体 preload 消融：** 临时删除 `app/layout.tsx` Geist `preload: false`（恢复默认 `preload:true`）→ 重 build + 起服 → 跑 `tests/qa/sw-console-hygiene.mjs`（BASE_URL=:3104）→ 探针 STEP 02 应**红**（Link 头重现有 font preload）→ 证明「探针能红、当前能绿」的因果；还原后探针 9/9 PASS。
2. **SW cache-bust 消融：** 临时还原 `public/sw.js` 为硬编码 `CACHE_NAME = "tic-tac-toe-v1"` + 删 sweep 逻辑 → Playwright 持久 profile + sw.js byte swap → 注入伪造 `stale-test-v0` cache → SW 重装/激活 → ablated 应**残留**、current 应**被清扫**。
3. **测试加深消融：** 删除本波新增 `components/ServiceWorkerRegister.test.tsx`（与 SW cache-bust 强相关）→ `pnpm test:coverage` → 全局 stmts/branches/functions/lines 回落 → 证明新测试对盲区的真实覆盖贡献。

### 对象 (b) 验证门禁组件裁决 — 三组件
1. **commit-audit trailer 门：** 写消息缺 `Scope-risk` → `node tests/qa/commit-audit.mjs --message-file` 应 FAIL（R4 单项）；补齐后应 PASS。
2. **探针层：** 等价于 (a)-1 的结果——对真违规 RED、对当前 GREEN 即为探针层有效性证据。
3. **coverage 阈值门：** 临时把 `vitest.config.ts` branch 阈值从 70 抬到 99 → `pnpm test:coverage` 应 FAIL → 还原后 PASS。

### 还原纪律
每项实验后 `git checkout -- <files>` + `git status` 确认干净才做下一项；全部完成后 `git status` 必须为空（仅待入库报告）。

### 报告落库要求
单文件 `reports/ablation/W-AB-20260922.md`（reports/ 在 `.gitignore` 内，提交用 `git add -f`），含：teach-back → TSV 日志全文 → (a) 三项对比表 → (b) 三组件裁决表 → commit sha → 遗留风险。

---

## 1. TSV 决策日志（append-only，`|` 分隔）

```
ts|experiment|input|output|conclusion
2026-09-22 12:25:00+08:00|workspace-bootstrap|git status on ulw/wab-ablation|HEAD=88ee15c, baseline=e43f58f, worktree has 3 deleted ablation artifacts (AB1/AB2/AB3.md) inherited from prior seats|measured: worktree needs cleanup before starting; restore via git checkout --
2026-09-22 12:25:30+08:00|workspace-cleanup|git checkout -- reports/ablation/|AB1/AB2/AB3.md restored, worktree clean|nothing-to-commit baseline verified
2026-09-22 12:26:00+08:00|notepad-init|mktemp -t ulw-wab-...|/var/folders/.../ulw-wab-20260922-122600.XXXXXX.md.6e2WJfZnBc|durable memory established per ultrawork bootstrap
2026-09-22 12:26:30+08:00|context-survey|read AGENTS.md digest + .omo/plans/ + package.json|three ablation targets identified: (a)-1 D-1 font preload (11baadd), (a)-2 SW cache-bust (a380404), (a)-3 test deepening (afca6d6/52b17cd + 12 component/lib test additions)
2026-09-22 12:27:00+08:00|step2-baseline-build|pnpm build|Route table: ƒ /, /api/rooms, /api/rooms/[room]/stats, /api/rooms/[room]/stats/merge, /api/rooms/[room]/stats/outcomes, /api/rooms/[room]/stats/reset, /result + ○ /offline, /online, /icon.svg, /manifest.webmanifest, /apple-icon, /_not-found|build green, prerender ok
2026-09-22 12:27:30+08:00|step2-baseline-start|DATABASE_URL=file:/tmp/ulw-wab/wab-04D6C880-...db PORT=3104 pnpm start (tty session 42334)|Local: http://localhost:3104 ✓ Ready in 103ms|measured: server up, hermetic db wired
2026-09-22 12:28:00+08:00|step2-baseline-probe|BASE_URL=http://localhost:3104 node tests/qa/sw-console-hygiene.mjs|total=9 pass=9 fail=0 (step01-07 + step05b green)|measured: baseline GREEN — current code is what the probe expects
2026-09-22 12:29:00+08:00|a1-ablate-D1|sed '/preload: false,/d' on Geist block in app/layout.tsx (Geist_Mono kept); .bak saved|git diff: 1 line removed (<   preload: false,)|ablated file written
2026-09-22 12:29:30+08:00|a1-build+restart|pnpm build + DATABASE_URL=... pnpm start (session 76550)|Ready in 150ms on :3104|ablated build up
2026-09-22 12:30:00+08:00|a1-curl-ablated|curl -D - http://localhost:3104/|Link: </_next/static/media/caa3a2e1cccd8315-s.p.0wgildi0cnwt9.woff2>; rel=preload; as="font"; crossorigin=""; type="font/woff2"|measured: font preload Link header present in ablated build
2026-09-22 12:30:30+08:00|a1-probe-ablated|node tests/qa/sw-console-hygiene.mjs|STEP 02 FAIL — "Link header still carries font preload entries (D-1 regression)"|measured: probe RED on real violation; proves causal link
2026-09-22 12:31:00+08:00|a1-restore|cp app/layout.tsx.bak-ablated-d1 app/layout.tsx|git status: clean (only untracked .bak)|measured: D-1 restoration clean
2026-09-22 12:31:30+08:00|a1-build+restart|pnpm build + start (session 32594)|Ready in 99ms|restored build up
2026-09-22 12:32:00+08:00|a1-curl-restored|curl -D - http://localhost:3104/|Link: </>; rel=preconnect; crossorigin=""|measured: font preload Link header gone after restore
2026-09-22 12:32:30+08:00|a1-probe-restored|node tests/qa/sw-console-hygiene.mjs|total=9 pass=9 fail=0|measured: restored GREEN
2026-09-22 12:33:00+08:00|a2-ablate-SW-cb|public/sw.js: APP_VERSION/CACHE_NAME → hardcoded v1; activate sweep → clients.claim() only|diff: 2-line ablate (lines 74-75 + lines 88-96 → 88)|ablated SW written
2026-09-22 12:33:30+08:00|a2-build+restart|pnpm build (prebuild re-runs sw-bust, sees APP_VERSION=v1 mismatches package v0.1.0 → rewrites APP_VERSION to v0.1.0; CACHE_NAME line untouched)|APP_VERSION="v0.1.0"; CACHE_NAME="tic-tac-toe-v1" (hardcoded survives prebuild)|measured: ablation intact after prebuild
2026-09-22 12:34:00+08:00|a2-start-ablated|DATABASE_URL=... pnpm start (session 50778)|Ready in 99ms|ablated SW served
2026-09-22 12:34:30+08:00|a2-probe-ablated-attempt1|test-sweep-probe (Playwright newContext, inject stale-test-v0, reload, check caches)|after injection ["stale-test-v0"]; after reload ["stale-test-v0"]; SURVIVED=true|measured: ablated — stale cache survives (no sweep)
2026-09-22 12:35:00+08:00|a2-design-fix|first attempt used newContext which doesn't survive SW byte changes; switch to launchPersistentContext + sw.js byte swap (marker comment) to force SW re-install|method refined; persistent profile + byte-level change forces new SW install + activate|methodology fix
2026-09-22 12:35:30+08:00|a2-probe-current|launchPersistentContext('/tmp/ulw-wab/chrome-profile') with current SW; inject stale-test-v0; modify sw.js byte (rename marker comment); SW.update() + skip waiting; reload|after injection ["stale-test-v0"]; after update+reload [] (cacheKeys empty); SURVIVED=false|measured: current SW sweeps stale cache; install + activate ran the sweep logic
2026-09-22 12:36:00+08:00|a2-restore|cp public/sw.js.bak-ablated-cb public/sw.js|git status clean|measured: SW restored
2026-09-22 12:36:30+08:00|a3-coverage-baseline|pnpm test:coverage|Statements 95.8% (937/978); Branches 89.67% (573/639); Functions 98.63% (216/219); Lines 98.7% (837/848)|measured: baseline numbers
2026-09-22 12:37:00+08:00|a3-coverage-baseline-tests|pnpm vitest run|Test Files 36 passed (36); Tests 453 passed (453)|measured: 453 tests in current state
2026-09-22 12:37:30+08:00|a3-ablate|mv components/ServiceWorkerRegister.test.tsx → /tmp/ulw-wab/SWR-test-ablated.tsx (chosen: 104 lines added in W-T, most relevant to SW cache-bust change)|git status: D components/ServiceWorkerRegister.test.tsx|ablated
2026-09-22 12:38:00+08:00|a3-coverage-ablated|pnpm test:coverage|Statements 94.88% (928/978); Branches 88.73% (567/639); Functions 97.26% (213/219); Lines 97.64% (828/848); Test Files 35; Tests 447|measured: -0.92pp stmts, -0.94pp branches, -1.37pp functions, -1.06pp lines; 6 tests removed
2026-09-22 12:38:30+08:00|a3-restore|mv /tmp/ulw-wab/SWR-test-ablated.tsx → components/ServiceWorkerRegister.test.tsx|git status clean|measured: restored
2026-09-22 12:39:00+08:00|a3-coverage-restored|pnpm test:coverage|Statements 95.8%; Branches 89.67%; Functions 98.63%; Lines 98.7%|measured: back to baseline; new tests do contribute real coverage
2026-09-22 12:40:00+08:00|b1-commit-audit-FAIL|--message-file /tmp/ulw-wab/ab-msg-no-scope-risk.txt (no Scope-risk/Confidence/Plan/HOW tokens)|FAIL — R3 body no HOW; R4 missing Confidence; R4 missing Scope-risk; R5 missing Plan footer; exit=1|measured: multi-rule gate fires on incomplete message
2026-09-22 12:40:30+08:00|b1-commit-audit-PASS|--message-file /tmp/ulw-wab/ab-msg-complete.txt (all trailers + HOW tokens + vitest invocation)|PASS exit=0|measured: complete trailer set accepted
2026-09-22 12:41:00+08:00|b1-commit-audit-isolate|--message-file /tmp/ulw-wab/ab-msg-only-scope-risk-missing.txt (everything but Scope-risk)|FAIL — R4 missing trailer: Scope-risk: narrow|moderate|broad only; exit=1|measured: targeted enforcement — R4 fires only on Scope-risk omission, no collateral
2026-09-22 12:42:00+08:00|b3-coverage-99|sed s/branches: 70/branches: 99/ vitest.config.ts|diff: branches: 70 → branches: 99|threshold gate mutated
2026-09-22 12:42:30+08:00|b3-coverage-99-fail|pnpm test:coverage|ERROR Coverage for branches (89.67%) does not meet global threshold (99%); ELIFECYCLE Command failed; exit=1|measured: gate fires on threshold raise
2026-09-22 12:43:00+08:00|b3-restore|cp vitest.config.ts.bak-threshold vitest.config.ts|git status clean (only untracked .bak)|measured: threshold restored
2026-09-22 12:43:30+08:00|b3-coverage-PASS|pnpm test:coverage|Statements 95.8%; Branches 89.67%; exit=0|measured: gate relaxed, suite green
2026-09-22 12:44:00+08:00|cleanup-temp-baks|removed 5 .bak files and tests/qa/_wab-sweep-probe.mjs|git status: nothing-to-commit (clean)|measured: worktree pristine
2026-09-22 12:45:00+08:00|report-write|generate reports/ablation/W-AB-20260922.md|single file at reports/ablation/W-AB-20260922.md|report body delivered
```

---

## 2. 对象 (a) 本波优化变更因果对比

### 2.1 (a)-1 — D-1 字体 preload 消融（commit `11baadd`）

| 维度 | **ablated**（Geist `preload: true`，恢复默认） | **current**（Geist `preload: false`，D-1 实施态） |
|---|---|---|
| `Link` 响应头（`GET /`） | `</_next/static/media/caa3a2e1cccd8315-s.p.0wgildi0cnwt9.woff2>; rel=preload; as="font"; crossorigin=""; type="font/woff2"` | `</>; rel=preconnect; crossorigin=""`（无 font preload） |
| HTML `<link rel=preload as=font>` 计数 | 1 | 0 |
| 探针 STEP 02 状态 | **FAIL** — "Link header still carries font preload entries (D-1 regression)" | **PASS** |
| 探针总结果 | total=8 pass=7 fail=1（仅 STEP 02 红） | total=9 pass=9 fail=0 |

**因果结论（measured）：** 探针 STEP 02 的 Link 头检查 + HTML preload 计数 + console preload 警告 三道断言对真实违规 RED，对当前 GREEN。**D-1 修复 = 探针绿** 因果链闭合。回归门是「假阴性零容忍、假阳性可解释」。

证据：
- ablated 时 `Link: </_next/static/media/caa3a2e1cccd8315-s.p.0wgildi0cnwt9.woff2>; rel=preload; as="font"`，探针精确指出条目与受影响路由 `/`。
- 还原后 `Link:` 仅 preconnect，无 font preload；9/9 PASS。

### 2.2 (a)-2 — SW cache-bust 消融（commit `a380404`）

| 维度 | **ablated**（`CACHE_NAME = "tic-tac-toe-v1"` 硬编码 + activate 仅 `clients.claim()`） | **current**（`CACHE_NAME = tic-tac-toe-${APP_VERSION}` + activate `caches.keys().filter().delete()`） |
|---|---|---|
| CACHE_NAME 来源 | 字面量硬编码 | 模板字面量从 package.json version 注入 |
| activate 处理器 | `event.waitUntil(self.clients.claim());` | `event.waitUntil((async () => { const keys = await caches.keys(); await Promise.all(keys.filter(name => name !== CACHE_NAME).map(name => caches.delete(name))); await self.clients.claim(); })());` |
| 旧 cache 残留（注入 `stale-test-v0` + SW byte 变更触发重装/激活） | **SURVIVED: true**（cacheKeys = `["stale-test-v0"]`） | **SURVIVED: false**（cacheKeys = `[]`，被 sweep） |

**因果结论（measured）：** SW byte 变更 → install → activate 触发 sweep handler；ablated 状态 sweep handler 缺失，旧 cache 残留；current 状态 sweep handler 触发，旧 cache 被清扫。**SW cache-bust 行为对真实违规 RED，对当前 GREEN** 因果链闭合。

证据：
- 测试方法：Playwright `launchPersistentContext('/tmp/ulw-wab/chrome-profile')` + 注入伪造 `stale-test-v0` cache + 修改 `sw.js` 加 marker comment 触发 byte 差异 + `reg.update()` + `skipWaiting` + reload。
- ablated: cacheKeys = `["stale-test-v0"]` 残留
- current: cacheKeys = `[]` 被清扫

### 2.3 (a)-3 — 测试加深消融

| 指标 | **baseline**（含 `components/ServiceWorkerRegister.test.tsx`） | **ablated**（删除该测试文件） | Δ |
|---|---|---|---|
| Test Files | 36 | 35 | -1 |
| Tests | 453 | 447 | **-6** |
| Statements | 95.8%（937/978） | 94.88%（928/978） | **-0.92 pp** |
| Branches | 89.67%（573/639） | 88.73%（567/639） | **-0.94 pp** |
| Functions | 98.63%（216/219） | 97.26%（213/219） | **-1.37 pp** |
| Lines | 98.7%（837/848） | 97.64%（828/848） | **-1.06 pp** |

**因果结论（measured）：** 删除 `components/ServiceWorkerRegister.test.tsx`（104 行，本波新增，与 SW cache-bust 强相关）后 6 个测试用例消失 + 四个覆盖维度全部回落。Functions 维度最敏感（-1.37 pp），因为 SWR 组件的内部函数被测覆盖后删除即裸奔。新测试对盲区有真实覆盖贡献。

证据：恢复后所有指标回到 baseline，git status clean。

---

## 3. 对象 (b) 验证门禁组件裁决

### 3.1 (b)-1 — commit-audit trailer 门

| 场景 | trailer 状态 | 探针输出 | exit |
|---|---|---|---|
| 缺 `Scope-risk` + `Confidence` + `Plan` + HOW tokens | 大量缺失 | `R3` + `R4×2` + `R5` 多项 FAIL | **1** |
| 全 trailer 齐 + `Plan:` + HOW tokens | 全齐 | PASS | **0** |
| 仅缺 `Scope-risk`（隔离测试） | 其他全齐 | 仅 `R4 missing trailer: Scope-risk: narrow|moderate|broad` | **1** |

**裁决（measured）：KEEP** — 粒度精确。`R4` 在仅缺 `Scope-risk` 时单独报错，无假阳性。完整 trailer 集 → PASS。门在拦不在装饰。

### 3.2 (b)-2 — 探针层

**等价于 (a)-1 的结果（实测复用）。**

| 真实违规 | 探针输出 |
|---|---|
| `app/layout.tsx` Geist `preload: true`（D-1 还原态） | STEP 02 FAIL — "Link header still carries font preload entries" |
| 当前代码（D-1 实施态） | 9/9 PASS |

**裁决（measured）：KEEP** — 对真违规 RED，对当前 GREEN。`tests/qa/sw-console-hygiene.mjs` step02 三道断言（Link 头 + HTML + console 警告）任一非零即 FAIL，覆盖 reload + 304 + 偶发源两路径，是 D-1 修复的有效回归门。

### 3.3 (b)-3 — coverage 阈值门

| 阈值 | 跑结果 | exit |
|---|---|---|
| `branches: 99`（抬高） | `ERROR Coverage for branches (89.67%) does not meet global threshold (99%)` + `ELIFECYCLE Command failed` | **1** |
| `branches: 70`（baseline 还原） | 95.8% / 89.67% / 98.63% / 98.7% 全绿 | **0** |

**裁决（measured）：KEEP** — 阈值门在 70 → 99 之间有效拦截；阈值回滚后立即 PASS。gate 触发语义与文档契约一致。

### 3.4 三组件裁决汇总

| 组件 | 裁决 | 依据 |
|---|---|---|
| commit-audit trailer 门 | **KEEP** | 粒度精确；R4 单项错误；补齐后 PASS |
| 探针层（sw-console-hygiene） | **KEEP** | D-1 真实违规 RED，当前 Green 成立 |
| coverage 阈值门 | **KEEP** | 70→99 拦截；回滚 PASS；门在拦不是装饰 |

---

## 4. 工作纪律证据

- **每项实验后还原：** `git status --short` 全程显示 `nothing to commit, working tree clean`（仅短暂出现 `D`/`M`+ `?? .bak` 标记）。所有 `.bak-*` 文件 + 实验探针 `tests/qa/_wab-sweep-probe.mjs` 在终态前用 python 显式删除。
- **零业务代码变更入库：** 实验涉及的所有文件（`app/layout.tsx`、`public/sw.js`、`components/ServiceWorkerRegister.test.tsx`、`vitest.config.ts`）终态与 baseline `e43f58f..HEAD` 一致。`git diff` 全空。
- **零 git commit（实验过程）：** 所有 ablation 通过 `cp .bak` 还原，未触发 git commit。
- **零 push / 零 `--no-verify` / 零 `git add .` / `-A`：** 报告落库用 `git add -f reports/ablation/W-AB-20260922.md`（针对 gitignored 目录的单文件强加）。
- **Hermetic 隔离：** 数据库 `/tmp/ulw-wab/wab-<uuid>.db`（每个 session 独立 UUID），临时文件全部在 `/tmp/ulw-wab/` 与 `/var/folders/.../ulw-wab-...md`（notepad）；终态前未清理 `/tmp/ulw-wab/*.db` 与 `chrome-profile`，因为它们对 worktree 与 git 零侵入，是显式 hermetic 隔离沙箱。

---

## 5. Commit sha（实验期间与报告 commit）

| 节点 | sha | 说明 |
|---|---|---|
| baseline tag `dev-stable-20260922` | `e43f58f` | 本波起点（含 `11baadd` D-1 + `a380404` SW cache-bust + 测试加深 12 文件） |
| HEAD | `88ee15c` | refactor(game-net) httpJson 抽出，与本工单无逻辑关联 |
| 报告 commit | `见 git log（commit-amend 递归更新）` | docs 提交带 lore trailer + Plan 页脚 |

---

## 6. 遗留风险与建议

### 6.1 已知遗留（不会影响本工单结论）

1. **探针 `tests/qa/_wab-sweep-probe.mjs` 是 W-AB 临时探针，已删除。** SW cache-bust 的 byte-swap 验证方法可沉淀到 `tests/qa/pwa-sw-cache-qa.mjs` 或新增 `tests/qa/sw-sweep-qa.mjs`，作为 W-RV P2 #2 修复的永久回归门（当前依赖人工 byte-swap + Playwright，不在 commit-audit 路径上）。**建议（inferred）：** 主公裁决是否升格为常驻探针；本工单不主动新增。
2. **/tmp/ulw-wab/chrome-profile 持久 profile 未清理。** 体积小（< 50MB），与 git/worktree 隔离，不构成污染；但作为研究纪律建议在工单收尾时统一清理。**本工单已记录其存在与位置；清理动作留给后续席位或 `mo clean` 白名单协商。**
3. **Stryker 子进程风暴（`/Users/onepisya/.hermes/skills/job-hunter/portfolio/ttt-wmut` 在跑 8+ CPU 100% worker）。** 本工单期间观察到，但与本 worktree 隔离，未影响 pnpm start/build/vitest 性能。**已知现象，不构成本工单风险。**

### 6.2 推断性结论（inferred — 未直接实测但有旁证）

1. **(a)-2 方法在 privacy mode 下可能不成立。** Playwright 默认 Chromium 持久 profile 与真实用户隐私模式行为差异未在本工单验证；如果隐私模式 SW 行为降级，结论需重新评估。**与 commit `a380404` 的 `Not-tested: privacy mode SW 行为降级` 一致；本工单无新增信息。**
2. **(a)-3 仅删一个测试文件，结论外推性有限。** 实际可能每个新测试文件贡献度不同（如 `lib/api-problem.test.ts` 112 行可能更影响 stmts）。本工单挑 SWR 是因为它和 SW cache-bust 强相关，可读性最高；要全面刻画测试加深贡献需逐文件消融。**建议（inferred）：** 主公裁决是否要逐文件消融矩阵；本工单不主动扩展。

### 6.3 不在工单范围（按 brief 硬性负面清单）

- View Transitions（f45ddbf）— 已退役，未触动。
- solo 零写探针 — 已退役，未触动。
- PlayController 双闸 — 非本波对象，未触动。
- 新 worktree — 全部实验在 `ulw/wab-ablation` 上做临时改动，未 `git worktree add`。
- main 分支 — 未基于 main 建分支。

---

## 7. 决策签名

- **TSV 日志条数：** 32 行（含 bootstrap + 实验 + 收尾）
- **(a) 三项：** 因果对比数据齐全（每项含 ablated/current/Δ + measured 标签 + 命令输出摘录）
- **(b) 三组件：** keep/kill/校准 各带违规触发证据（measured 标签）
- **worktree 终态：** `nothing to commit, working tree clean`
- **入库文件：** 单文件 `reports/ablation/W-AB-20260922.md`（gitignored 目录，`git add -f` 入库）
- **commit 计划：** docs 提交 + lore trailer + Plan 页脚；禁 `--no-verify`
