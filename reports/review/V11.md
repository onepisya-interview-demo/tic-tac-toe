# V11 Review — ulw-result-win-celebration（fresh-context, adversarial）

- **日期**：2026-09-21
- **裁决**：**ACCEPT**
- **验审范围**：detached @2d8f8d4（合并后 dev HEAD），基线 0ba2dc9（plan 入档），`git diff 0ba2dc9..HEAD` = 37 文件 +1540/−132 行
- **合并完整性**：`git log 0ba2dc9..HEAD --oneline` 实测 **6 commits**（任务书面写「7」，其括号分解 wb 2 + wc 2 + wa 1 + wa2 1 = 6 与实测一致，判定为任务书笔误；提交序 wb→wc→wa→wa2 可从 commit message 辨认：0483513/133397c（wb）、5fe93c0/adab290（wc）、1c3899e（wa）、2d8f8d4（wa2））——measured
- **本席职责**：fresh-context 对抗终验；未参与本波任何一行代码，未读四席报告。只对真实产物取证：真实 diff、真实命令、真实 HTTP。Read-only on source；唯一产出：本报告。
- **标签约定**：每条结论同一句带 measured（实测）/ inferred（代码对照）/ guess（仅推断）标签 + file:line / 命令输出指针。

---

## 总裁决理由（一句话）

四波交付与计划 §1-§4b 逐条对得上：W-A 哨兵「仅 won 写 / 读后即清 / 恰消费一次 / 刷新书签不重放」在代码层唯一写点成立并经探针与跨波集成实证；W-A2 红线四文件注释-only；W-B 红线断言零改动；W-C 弃 slug 现役树清零且四端点 curl 实弹全中；六层门禁 + 7 探针 + 2 跨波集成全绿；仅存 NOTE 级事项（任务书 commits 笔误、AGENTS.md 代码地图缺行为已声明预期、glossary 历史 plan 豁免命中），无 BLOCKER/MAJOR/MINOR。

---

## F1-F12 逐条判定表

| # | 断言 | 方法 | 证据指针 | 判定 |
|---|------|------|---------|------|
| F1 | online 胜局（正规路径）→ /result confetti 恰一次（1.1s 窗）；哨兵读后即清 | 探针 result-celebration-qa F1（:3115 生产构建）；跨波集成 I-1 复验 | `STEP: F1 ... PASS 2312ms`；`[data-testid=confetti]` count=1 + `result-celebration` count=1 + fixed canvas ≥1 + 哨兵 `sessionStorage.getItem → null`（tests/qa/result-celebration-qa.mjs:98-110 / /tmp/ulw-v11/integration.mjs I-1）——measured | **PASS** |
| F2 | 刷新 /result 不重放 | 探针 F2 + 集成 I-1 reload 步 | `STEP: F2 reload /result → no replay ... PASS`；I-1 reload 后 confetti=0——measured | **PASS** |
| F3 | 首页「查看战绩」/书签直达 /result 不触发庆祝 | 探针 F3（bookmark 带 row 直达） | `STEP: F3 bookmark /result?room= with existing row → no celebration ... PASS`——measured | **PASS** |
| F4 | online 平局无哨兵无庆祝仍推 /result | 探针 F4 + 单测 ResultNavigator.test.tsx:215-247（drawn 四场景 `getItem → null` 断言） | `STEP: F4 online draw → no sentinel, no celebration, still pushes /result ... PASS`——measured | **PASS** |
| F5 | reduced-motion 无动画路径其余语义不变 | 探针 F5 | `STEP: F5 reduced-motion → confetti layer DOM present, no canvas burst, sentinel still cleared ... PASS`——measured | **PASS** |
| F6 | offline 全路径回归 | 探针 offline-qa 5/5 + offline-mode-qa 9/9 + anonymous-first-game-qa 6/6 | SUMMARY 行（见门禁区块）——measured | **PASS** |
| F7 | 直达 /online 有名 → bootstrap → 可玩 → 胜局全流程 | 探针 online-direct-qa F7 + 集成 I-1 | `STEP: F7 ... PASS 1124ms`；I-1 hard load 无弹框 + outcome POST 恰 1 + server totalGames=1——measured | **PASS** |
| F8 | 直达 /online 无名 → 页内收名 → 全流程，零静默无名对局 | 探针 online-direct-qa F8 + 集成 I-2 | `STEP: F8 ... PASS 1443ms`（gate [open] → 提交前零 /api/* → force click 落不进子 → 提交 → 可玩 → 胜局）；I-2 ESC/遮罩拒绝不放行——measured | **PASS** |
| F9 | 首页路径回归，门流程零回退 | 探针 home-return-qa 9 步 + one-identity-qa 10 步 + W-A2 红线四文件 diff 甄别 | 全 PASS（见门禁区块）；四文件 diff 全部注释-only（见对抗甄别 A2）——measured | **PASS** |
| F10 | 4 端点错误分支新 slug + content-type 恒 problem+json | curl 实弹对 :3115（见 C 区块） | 422 invalid-room-name / 404 stats-not-found title='Room stats not found' / 409 enter-room-required title='Enter room required'，四条响应头 `content-type: application/problem+json`——measured | **PASS** |
| F11 | 旧 slug 全仓清零 | `grep -rn "player-session-required\|invalid-player-name" .`（带豁免与不带豁免两遍） | 现役树（代码/tests/docs/README）0 命中（exit=1）；豁免目录 `.omo/plans/` 内 5 处（plan 原文自身 + glossary-context-md.md 历史 W1 记录）——measured | **PASS** |
| F12 | vitest 全绿 + 五探针回归 + QA 零 slug 直引 | vitest 343/343；`grep slug tests/qa/` exit=1 | 29 文件 343 测试全 PASS（4.38s）；QA 探针零直引 slug——measured | **PASS** |

---

## 门禁结果（六层，全部在 ttt-v11 worktree 实测）

| 层 | 命令 | 结果 |
|----|------|------|
| ① vitest | `pnpm vitest run` | **29 文件 / 343 测试全 PASS**（4.38s；V10 基线 325，+18 = 本波新单测 ResultCelebration.test / ResultNavigator.test / OnlineGateMount.test + rooms*.test 期望值更新）——measured |
| ② typecheck | `pnpm typecheck` | 0 errors——measured |
| ③ lint | `pnpm lint` | 0 errors 0 warnings——measured |
| ④ build | `pnpm build` | compiled successfully；4 API 路由 ƒ dynamic + /result ƒ + /online ○ + /offline ○——measured |
| ⑤ commit-audit | `node tests/qa/commit-audit.mjs --branch dev` | **branch=dev total=302 pass=296 skip=6 fail=0**，8 条本波 commit 全 PASS——measured |
| ⑥ QA 探针 | BASE_URL=:3115 生产实例（`DATABASE_URL=file:/tmp/ulw-v11/v11-*.db PORT=3115 pnpm start`） | 7 探针全绿（下表）——measured |

探针明细（全部 `BASE_URL=http://localhost:3115`）：

| 探针 | 结果 | 关键步骤 |
|------|------|---------|
| offline-qa.mjs | 5 PASS / 0 FAIL | 含 A4 named mount 零网络 + 8 stale testid 反向断言 |
| offline-result-qa.mjs | 5 PASS / 0 FAIL | /result 三分支 + ?name= fallback |
| one-identity-qa.mjs | 10 PASS / 0 FAIL | 含 **q6 legacy key cleared-on-mount**（W-B 红线断言实证仍工作）、q1a/q1b/q1c 首页零 API |
| home-return-qa.mjs | 全 PASS（9 步） | 含 step09 legacy /api/sessions + /api/players/* 404 |
| offline-mode-qa.mjs | 9 PASS / 0 FAIL | 「12 total」= findings 数组含 3 条 retired/skip 记录（:429 `findings.length`，与 V10 基线一致），非失败 |
| result-celebration-qa.mjs（新） | 5 PASS / 0 FAIL | F1-F5 全绿 |
| online-direct-qa.mjs（新） | 2 PASS / 0 FAIL | F7/F8 全绿 |
| anonymous-first-game-qa.mjs（新） | 6 PASS / 0 FAIL | S1-S6：零预置名 → /offline 首局 → 账本 0→1 → 回首页 `sync-confirm-dialog[open]` → 提交前零 /api/* |

---

## A1 对抗甄别：W-A 哨兵语义（代码层）

- **唯一写点**：`grep -rn "JUST_WON_SENTINEL_KEY\|ttt.result.just-won"` 全仓——生产代码 setItem 仅 `components/ResultCelebration.tsx:33`（`writeJustWonSentinel` 内），唯一调用方 `components/ResultNavigator.tsx:84`；测试/探针仅引用 key 做断言——measured。
- **仅 won 写（D-2）**：哨兵写在 `if (phase === 'won')` 块内（ResultNavigator.tsx:81-85），位于完整守卫链之后：`mode !== 'online'` 早退（:74）→ `prev === null` 早退（:75）→ 非 terminal 早退（:76-77）→ prev 已 terminal 早退（:78，witnessed-transition 语义）→ room 空早退（:79-80）。drawn 走 push 但不写哨兵——inferred（代码对照）+ F4 探针 measured。
- **读后即清、恰消费一次**：`consumeJustWonSentinel()`（ResultCelebration.tsx:46-55）getItem 与 removeItem 同步完成；消费点唯一在 mount effect（:98-109，空依赖数组），state 仅 false→true 单向，二次 effect run 找不到哨兵为 no-op；`app/result/page.tsx` 两个 return（fallback :92 + branches :159）都挂岛，保证任何 /result 加载必消费 stale 哨兵、泄漏到后续书签访问不可能——inferred（代码对照）。
- **SSR/隐私模式安全**：write/consume 均 typeof-window 守卫 + try/catch，降级为 plain scorecard（ResultCelebration.tsx:30-38, 52-54）；服务端首帧 celebrate=false 无 hydration mismatch——inferred。
- **StrictMode 双跑**：dev 双 effect 下第二跑 consume 返回 false；production 无双跑——inferred（组件头注释 :81-88 + 单测绿）。

## A2 对抗甄别：W-A2 红线四文件注释-only

`git diff 0ba2dc9..HEAD` 逐行核验（measured）：

| 文件 | 改动 | 甄别 |
|------|------|------|
| app/page.tsx | 2 行（±1 对） | 全在 JSDoc 注释内（「W2 客户端波退役，历史记录」/「收名现走 RoomGateDialog」），零代码行 ✓ |
| components/RoomGateDialog.tsx | 3 行（±1 对 +1） | JSDoc 措辞修正（PlayerNameForm 退役波次勘误），零代码 ✓ |
| components/RoomGateMount.tsx | 13 行（reflow） | 同段 JSDoc reflow，语义不变，零代码 ✓ |
| components/StartGameButton.tsx | 3 行 | 注释补「playerName 拼写退役」，零代码 ✓ |

W-B 触碰的其余组件同理核验：HomeDialogMount / ResetStatsButton / SyncConfirmDialog / lib/store.ts 全部注释-only（JSDoc 措辞 + 退役史勘误），零代码行——measured。

## A3 对抗甄别：W-B 红线断言零改动

- `tests/qa/one-identity-qa.mjs` 全文件 diff = **仅 :416 一行注释**（Q6 注释措辞）；`LEGACY_KEY` 常量（:17 `"ttt.player.name.v1"`）与 Q6 step（:425-433）零改动——measured。q6 探针在 :3115 实测 PASS（legacy 键 mount 期清除仍工作）。
- `tests/qa/offline-qa.mjs` 全文件 diff = **仅 :362 一行注释**（A4 注释房间术语化）；:404-421 反向断言七连（offline-sync / sync-confirm-dialog / offline-stats-merge-note / offline-stats-error / player-name-section / room-gate-dialog / online-stats-grid 全 `must be absent`）+ 「单机战绩」heading 断言逐字在位——measured。
- 其余 .mjs（offline-mode-qa / one-screen-qa / sync-qa / visual-qa）diff 全部注释-only，逐行看过：无断言、无选择器、无流程改动——measured。
- 新探针 anonymous-first-game-qa.mjs 为 D-5a 白名单内新增文件（240 行）✓。

## A4 对抗甄别：W-C 红线

- **grep 双遍**：① 带 `--exclude-dir` 豁免跑 → exit=1 零命中；② 不带豁免跑 → 命中全部落在 `.omo/plans/`（豁免清单：node_modules / .next / .omx / .git / .omo / reports / .zcode / .delta / .vitest-tmp / .stryker-tmp）。具体命中：`ulw-result-win-celebration.md`（计划原文自身，F11 定义字符串）+ `glossary-context-md.md:86,132,140`（W1 时代 /api/players/* 历史记录，非现役契约）——measured。
- **新 slug 弃词检查**：`lib/api-problem.ts` 现役 ProblemSlug 七值（stats-not-found / enter-room-required / invalid-room-name / invalid-request-shape / invalid-json / method-not-allowed / db-unavailable）与 TITLES 全表——零 session/solo/login/register 弃词；TYPE_BASE 前缀 `https://docs.example.com/probs/` 不变；`problemResponse` 硬钉 `content-type: application/problem+json`（lib/api-problem.ts:106-112）——measured。
- **契约文档同步**：CONTEXT.md 2 处 slug 字符串（merge 409 + 防静默建档条）、AGENTS.md 3 处（代码地图 endpoints 行 + SyncConfirmDialog 行 + 409 反模式条）diff 逐行核验同步完好——measured。
- **DESIGN.md 两波改动无互覆**：:169 行 W-C slug 修订（`409 enter-room-required`）与 :384/:392 行 W-B 直显语义改写（V10 MINOR-2 建议稿落地）相隔 200+ 行，合并后两处均完好——measured。

## A5 W-C curl 实弹（:3115 生产实例）

| 场景 | 命令 | 实测响应（原文） | 判定 |
|------|------|----------------|------|
| C1 非法房名 | `curl -X POST /api/rooms -d '{"room":"v…v"}'`（25 chars） | `HTTP/1.1 422` + `content-type: application/problem+json` + `{"type":"https://docs.example.com/probs/invalid-room-name","title":"Invalid room name","status":422}` | ✓ measured |
| C2 不存在房间 GET stats | `curl /api/rooms/v11-nonexistent-xyz/stats` | `HTTP/1.1 404` + problem+json + `{"type":"…/stats-not-found","title":"Room stats not found","status":404,"detail":"No row for room \"v11-nonexistent-xyz\"."}` | ✓ measured |
| C3 未进房 merge（先 POST /api/rooms 建 v11-merge-seed 得 200，再对 v11-merge-victim merge） | `curl -X POST /api/rooms/v11-merge-victim/stats/merge -d '{"stats":{…}}'` | `HTTP/1.1 409` + problem+json + `{"type":"…/enter-room-required","title":"Enter room required","status":409,"detail":"No row for room \"v11-merge-victim\". Enter or create the room before merging."}` | ✓ measured |
| C4 不存在房间 outcomes（合法 body `{"outcome":"X"}`） | `curl -X POST …/stats/outcomes` | `HTTP/1.1 404` + problem+json + `{"type":"…/stats-not-found","title":"Room stats not found",…}` | ✓ measured |
| 附：outcomes 422/200 分支 | 非法 body `{"outcome":"x"}` / 合法 body 对存在房间 | 422 invalid-request-shape；200 `{"stats":{"totalGames":1,…,"draws":1,…}}` | ✓ measured |

注：C4 首打用 `{"outcome":"x"}` 得 422——系本席 body 构造错误（校验顺序 shape 先于 row-probe，符合 transport 契约），改合法 body 后 404 如约；顺带实证 422 分支在位。

## A6 跨波集成（/tmp/ulw-v11/integration.mjs，复用 tests/qa/lib/browser.mjs）

- **I-1（W-A2 bootstrap × W-A 哨兵联合路径）PASS 2513ms**：直达 /online hard load（绕过首页门）→ 无弹框（bootstrap 生效）→ 胜局前哨兵 null → 0,3,1,4,2 → outcome POST 恰 1 → /result confetti 层=1 + celebration 层=1 + fixed canvas ≥1 → 哨兵 null（读后即清）→ reload 后 confetti=0（不重放）——measured。
- **I-2（无名 ESC 不放行）PASS 2154ms**：直达 /online 无预置 → `room-gate-dialog[open]` → ESC → 仍 [open] → force click cell-4 无 mark（showModal inert）→ 遮罩点击仍 [open] → 全程 `/api/*` 请求数 0 → 棋盘 9 格在位（假阴性排除）——measured。代码层根因：RoomGateDialog cancel handler `e.preventDefault()` + `onReject()`（RoomGateDialog.tsx:103-108），OnlineGateMount.handleReject 刻意 no-op（OnlineGateMount.tsx:106-108）——inferred 与 measured 互证。

---

## 发现清单

### BLOCKER
无。

### MAJOR
无。

### MINOR
无。

### NOTE（不阻塞）

- **NOTE-1**：任务书面「`git log 0ba2dc9..HEAD` = 7 commits」与实测 6 不符；其括号分解（wb 2 + wc 2 + wa 1 + wa2 1）= 6 与实测一致，判定为任务书笔误，非交付缺陷——measured。
- **NOTE-2**：AGENTS.md 代码地图无 ResultCelebration / OnlineGateMount 行（任务已声明：调度者终验后统一 sync，预期状态，不计发现）。同理 components/AGENTS.md。顺带实证：AGENTS.md 其余行无漂移——measured。
- **NOTE-3**：`.omo/plans/glossary-context-md.md:86,132,140` 残留 3 处旧 slug `player-session-required`——位于豁免目录，内容是 W1 时代 /api/players/* 的历史台账记录（非现役契约描述）。若追求「全仓含 docs 零命中」的字面 F11，可下波给这三行加「（W-C 前旧 slug）」标注；现状按豁免判定 PASS——measured。
- **NOTE-4**：本机 node v22.23.1 vs engines 声明 24.x（pnpm WARN，V10 同环境已存在，非本波引入，门禁全绿不受影响）——measured。

---

## 未覆盖项（确定性分级明示）

- **F5 reduced-motion 的「动画确实未发射」**：探针断言到「无 fixed canvas」第 2 级确定性（DOM 无 canvas 等价于 burstConfetti 未执行的观测面）；未逐一反汇编 canvas-confetti 内部——inferred（探针语义充分）。
- **Turso HTTP 路径**：本验全程本地 file: sqlite（hermetic tmp DB）；Vercel/Turso HTTP 分支未实测（沿用既有 known-cached 议题 RC-drift §4.1 P1-2，非本波 scope）——guess。
- **并发/竞态**：merge 与 outcomes 的并发写未压测（单线程顺序实弹）；`registerOrLoginRoom` race-safe 声明沿用单测与既有实证——inferred。
- **422 分支的房名白名单全谱系**（控制字符/emoji/24 边界）：由 vitest rooms*.test.ts 覆盖 + 本席 25-char 实弹抽样，未穷举——inferred。
- **基线 0ba2dc9 复测**：未在基线重跑探针对照（探针回归对象是合并后 HEAD；同源单测与 diff 审计无功能性回退迹象）——inferred。

---

## 附：环境指纹

- **代码版本**：2d8f8d4d74fb9d90b7bbfa1cbd0f0a039818efe5（detached，worktree ttt-v11，工作区干净）
- **基线**：0ba2dc969043261dc4e2867f6df13843d99e5613（plan 入档）
- **runtime**：`DATABASE_URL=file:/tmp/ulw-v11/v11-1789955792-29623.db PORT=3115 pnpm start`（Ready in 102ms；终验后已停，`lsof -i :3115` = 0 监听）
- **vitest**：343/343 PASS（29 文件 / 4.38s）
- **typecheck / lint**：0 errors / 0 errors 0 warnings
- **build**：成功（Next.js 16 产线构建，worktree 内合法执行）
- **commit-audit**：fail=0（302 commits 全过）
- **QA 探针**：8/8 全绿（7 指定 + 1 跨波集成 2 步）
- **curl 实弹**：6 发全中（4 指定场景 + 422/200 附加分支）
- **一次性脚本**：/tmp/ulw-v11/{integration.mjs, server.log, db-url.txt}（不入仓）

## 入档

`reports/review/V11.md`（本文件，主 worktree 未跟踪新文件）。判决 **ACCEPT**。本席 idle，不 commit、不改代码。
