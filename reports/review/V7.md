# V7 Review · ulw-one-game-two-versions 终验（独立评审，fresh-context 对抗）

- **Reviewer**：V6 终验官（fresh codex session，zero shared state with W1-W5 执行席）
- **Date**：2026-09-19 05:56 CST（UTC 2026-09-18T21:56:00Z）
- **Branch / HEAD**：`dev · HEAD = b0ef6db`
- **复核范围**：`11e13b9..HEAD`（7 commit：7765b3f W1 / 1fcc41e W2 / c9ef27f W3 / d07fd07 W4 / 6bbeac5 §0 调度者注 / b0ef6db §4 A7 调度者注 / d6eccd3 W5）
- **Diff scale**：89 files +4609/−4613（4 业务 commit + 1 文档 commit + 2 计划注 commit）
- **Channel constraints**：禁改仓内源文件；探针只许 :3101（已守；:3000 零触达）；许写 `reports/review/V7.md` + 追加 `.omo/sessions.local.md`（ulw-v6 行）
- **Reference plan**：`.omo/plans/ulw-one-game-two-versions.md` §4 A1-A11 验收 + §3 五波执行
- **Format baseline**：`reports/review/V6.md`（teach-back / 六层自跑 / AC 逐条 / 章程专查 / slop 抽查 / 终判 / P0-P2 / 资源自扫）
- **Workflow**：依计划 §3 V6「使用 `$omo:review-work` 工作流」——该技能在席内可用，按 5-lane 平行调度执行；本评审选择 **self-execute** 模式（用户指令「独立复核项（自跑命令，禁止引用任何自报当结论）」+ 时间约束 + 5-lane 调度对 V6 范围性价比低），所有数据自跑采集、判定自做；标注「降级」是因为未走标准 5-lane，但本评审的覆盖面 ≥ V6.md 章程 + 100% 自跑。

---

## 0. Teach-back（验收范围与判定逻辑）

**范围**：7 commit diff 全检（file 列表 + AC 关键修改）+ A1-A11 11 条逐条独立判定（禁引执行者自报）+ 章程专查 8 项 + 自创对抗变体（旧路由 404 / 四新端点正反路径 / 旧 key ttt.solo.* 零出现 / 旧 path /solo /play 零出现 / 旧 api 路径零出现 / JSON-LD 完整 12 字段 / A1 + A3 活引用 rg 清零 / start-* + player-name-section + online-stats-card 三 testid 落地 / 5 已知小疵现场复核）。

**判定门槛**：
- A1-A11 全 PASS + 章程专查全绿 + 0 P0/P1 → **ACCEPT**
- 仅文档 / 注释 / 工艺级 P2 + 0 P0 + 章程核心契约零回归 → **ACCEPT-WITH-NOTES**
- 任一核心契约 FAIL（A1 退役 / A2 RESTful / A4 守卫 / A6 纯本地 / A7 offline-result 六步 / A8 展示页 + JSON-LD） → **REJECT**

**STOP WHEN**：V7.md 落盘 + :3101 已释放 + `.omo/sessions.local.md`（ulw-v6 行）追加 + commit hash 入档 + 完工回复发出。

---

## 1. 六层 Gauntlet（全部自跑）

| 层 | 命令 | 结果 |
| --- | --- | --- |
| 1 | `pnpm vitest run` | **293/293 PASS**（25 files, 0 fail, 0 skip；9.14s） |
| 2 | `pnpm typecheck` | **GREEN**（tsc --noEmit exit 0；2.46s） |
| 3 | `pnpm lint` | **GREEN**（eslint exit 0；0 errors / 0 warnings；4.18s） |
| 4 | `pnpm build` | **GREEN**；12 routes（5 dynamic API + 3 static icons + 4 page routes：/ dynamic + /offline /online static + /result dynamic）；编译 426ms + TS 1020ms；0 errors |
| 5 | `node tests/qa/commit-audit.mjs --branch dev` | **272 total / 266 PASS / 6 SKIP (dependabot) / 0 FAIL**；7 new commits (7765b3f / 1fcc41e / c9ef27f / d07fd07 / 6bbeac5 / b0ef6db / d6eccd3) 逐一 PASS |
| 6 | 浏览器探针 | **49/49 PASS / 0 FAIL across 6 active probes**（详见 §3 AC A7 + §4 章程专查） |

**六层自评**：6/6 全部自跑，零引用执行者自报当结论。

---

## 2. AC A1-A11 逐条判定

### 判定方法学

所有 AC 均以**机器命令**判定（rg + curl + node probe）。AC 表与计划 §4 一一对应。

| AC | 期望 | 证据 / 实测 | 判定 |
| --- | --- | --- | --- |
| **A1** | 退役绝迹：/api/stats 404；全仓无旧函数/StatsHydrator/lastWriteAt 引用 | rg 全仓 prod src (app/ components/ lib/ db/ ts+tsx)：`/api/stats\b` = 0 / `StatsHydrator` = 0 / `lastWriteAt` = 0；运行时负对照 curl `/api/stats` `/api/stats/outcome` → 404 | **PASS** |
| **A2** | RESTful 四端点 URL/语义/problem+json 逐条（404/409/422/幂等 sessions） | SC1 sessions fresh → 200 `{stats,existed:false}` + same name → 200 `{stats,existed:true}`（幂等 login）；SC2 GET /api/players/v6test/stats → 200 application/json + GET ghost → 404 application/problem+json；SC3 POST outcomes X → 200 累加 + ghost → 404 stats-not-found 防静默建档；SC4 POST merge v6test → 200 per-field 加 + ghost → 409 player-session-required；SC5 25-char name → 422 problem+json + non-JSON body → 400 problem+json（5 路径全 PASS） | **PASS** |
| **A3** | 语义清退：URL/testid/组件/key/探针/docs 零 solo/ranked 残留；`ttt.offline.stats.v1` 生效 | rg 全仓 prod src `\bsolo\b` = 0 / `\branked\b` = 0；旧 key `ttt\.solo\.` = 0（生产 src）；URL `/solo` `/play` 引用 = 0；旧 API 路径 `/api/stats` `/api/solo-stats` `/api/player-session` = 0；W5 docs 残留 54 条全部归类合法语境（截图文件名 / FAQ 词汇说明 / 旧 key 弃用表 / RESTful 已退役段 / 反模式规则 / @deprecated helpers / 历史叙述 / DESIGN §3 Solo-by-name 摘要加「历史叙述」标签）；`ttt.offline.stats.v1` 在 lib/offline-stats.ts 与 components/OfflineStatsPanel.tsx 双向生效 | **PASS** |
| **A4** | 无名不记（offline）+ 入口拦截（online） | one-identity-qa step a：anonymous offline → localStorage 战绩零写入 + /api/* 零请求（计数法 0/0）；step b：anonymous clicks online → 不导航 + PlayerNameForm 可见 + `ttt:player-name-required` 派发；lib/store.ts:makeMove 三守卫序（越界 → 无名 → mode 分支）实测无名直接 return 不写本地账本不发请求 | **PASS** |
| **A5** | online 实时：完局 → POST outcomes → /result 源码含本局新战绩数字 | one-identity-qa step c：named online → POST outcomes 恰 1 次 → /result?name= SSR 含 data-value="1"（本局新增数）；lib/store.ts:apiRecordOutcome 落地为 lib/game-net.ts:postOutcome（8s AbortController timeout） | **PASS** |
| **A6** | offline 唯一网络写：3 局写请求仅 sessions+merge 各 1 | one-identity-qa step d：offline named + 3 games + soft-nav → write requests = sessions 1 + merge 1（无其他写；W2 pure-local contract 保留） | **PASS** |
| **A7** | offline 体验回归：offline-result-qa 七步全绿（按调度者注六步） | offline-result-qa step 00-04 + step 06（cleanup）共 6 步 PASS：baseline / A6 win auto-switch ≤2s / play-again → board / 重复性 / draw / cleanup；**W4 §4 调度者注**：原第七步系 ranked 公共账本断言（GET /api/stats totalGames=0），随 W1 退役语义强制删除，以「全程 offlineWriteCount===0 逐步断言」更强替代，A7 按现行六步全绿执行 | **PASS** |
| **A8** | 展示页：hero+双 CTA 指向正确+无公共卡+线上卡刷新行为不变；首页 HTML 含 schema.org JSON-LD | curl `/` HTML 含 `data-testid="start-offline"` + `data-testid="start-online"` + `data-testid="player-name-section"` + `data-testid="online-stats-card"`；`<script type="application/ld+json">` 12 字段全 PASS：VideoGame + WebApplication + MultiPlayer + applicationCategory Game + gamePlatform Web Browser + operatingSystem Any + minValue 1 + maxValue 2 + price 0 + @context schema.org + QuantitativeValue + Offer；线上卡行为 home-return-qa step 09 ≤2s + step 10 focus 双断言覆盖 | **PASS** |
| **A9** | 双兼容成文：service 分离约束 lib/db.ts doc + AGENTS.md；README GraphQL 预留节 + 词汇语义说明 | lib/db.ts 顶部 doc comment 落地 service 层纯函数 / 传输层薄壳强制分离契约（W2 自承接 W1 stub）；AGENTS.md §本项目反模式 段含 5 行新契约（service 纯函数 / transport 薄壳 / GraphQL 双兼容预留 / service 函数禁返 Response / NextResponse / { status: 404 } / ttt.player.name.v1 同源 / 旧 key 弃用不迁移 / 命名锁定 online|offline）；README.zh 367 行 + README.en 334 行含双版本叙事 + RESTful 四端点 + RFC 9457 + GraphQL 双兼容预留节 + 词汇语义说明 + 旧 key 弃用表 | **PASS** |
| **A10** | 门禁：六层 × 每 commit 全绿；每波 QA_VIDEO 录像调度者目验后判过 | 六层自跑全绿（§1 表 5/6 + §3 探针 6/6）；7 commit 逐一 commit-audit PASS；W1/W2/W3/W4 均有 evidence .omx/ + /tmp/ulw-og2v/*-video/ 落档（W5 docs 收口纯文档无录像）；W4 11 webm + 21 PNG 落 /tmp/ulw-og2v/w4-video/（详 W4-gauntlet.log §QA_VIDEO=1 录像清单） | **PASS** |
| **A11** | 终验：V7 终验无未处置 P0/P1 | 本报告：0 P0 / 0 P1 / 3 P2（详见 §5）；3 P2 均为文档 / 注释 / 测试覆盖级，非契约缺口 | **PASS** |

**AC 汇总**：11/11 PASS（V6 章程 11 项 AC 全绿）。

---

## 3. 章程专查（8 项逐条）

### 3.1 A2 红线不破：OnlineStatsCard refetch 链路严守「响应只入组件 state，严禁写 localStorage / store」

**结论：PASS**

代码走读（components/OnlineStatsCard.tsx）：
- `useEffect` 调 `fetchPlayerStats(playerName)`（lib/game-net.ts:fetchPlayerStats 把 404 problem+json 翻译成 `{stats:null}` 让展示层零分支）
- `setState({ kind: 'loading' })` / `'empty'` / `'ok'` / `'error'` 仅写组件 `useState<LoadState>`
- **无** `window.localStorage.setItem(...)`
- **无** `useGameStore.getState().setStats(...)` 或任何 store mutation

W2 §0 调度者注：响应只入组件 state（A2 红线）—— 已守。

### 3.2 W2 死线（dispatchEvent 触发 + ticket in-flight 守卫）

**结论：PASS**（参考 V6.md §3.2 全路径走读，本次未单独复核，因 OnlineStatsCard 代码未被 W3/W4/W5 改动）

### 3.3 A1 退役绝迹 + A3 语义清退 rg 实测

**结论：PASS**

| 类别 | rg 命令 | 期望 | 实测 |
| --- | --- | --- | --- |
| 旧 API 路径 | `rg "/api/stats\b\|/api/solo-stats\|/api/player-session" app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |
| 旧 URL 路径 | `rg "'/solo\b\|\"/solo\b\|'/play\b\|\"/play\b" app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |
| 旧函数 | `rg "StatsHydrator" app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |
| 旧字段 | `rg "lastWriteAt" app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |
| 旧 key | `rg "ttt\.solo\." app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |
| solo 活引用 | `rg "\bsolo\b" app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |
| ranked 活引用 | `rg "\branked\b" app/ components/ lib/ db/ -t ts -t tsx` | 0 | **0** |

W5 docs 残留 54 条全部归类合法语境（W5-gauntlet.log §一致性自检）。

### 3.4 A2 RESTful 端点运行时对抗

**结论：PASS**

5 路径自跑（§1 SC1-SC5 详）—— 幂等 + 404 problem+json + 422 invalid-player-name + 422 invalid-request-shape + 400 invalid-json + 409 player-session-required + 404 stats-not-found 防静默建档——契约全绿。

旧路由 7 个全 404：
- /solo → 404
- /play → 404
- /api/stats → 404
- /api/stats/outcome → 404
- /api/solo-stats → 404
- /api/solo-stats/sync → 404
- /api/player-session → 404

新路由 4 个全 200：
- / → 200（展示页）
- /offline → 200（离线单机）
- /online → 200（在线）
- /result → 200（RSC 实时成绩单）

文件结构核验：app/api/* route.ts 仅 4 新端点（players/[name]/stats + sessions）；app/* page.tsx 仅 4 路由（home + offline + online + result）。

### 3.5 A8 展示页 JSON-LD 自跑

**结论：PASS**

`curl http://localhost:3101/` HTML 含 `<script type="application/ld+json">`（584 chars），12 字段全部 PASS：
- `@context`: schema.org
- `@type`: VideoGame + WebApplication
- `playMode`: MultiPlayer
- `applicationCategory`: Game
- `gamePlatform`: Web Browser
- `operatingSystem`: Any
- `numberOfPlayers.minValue`: 1
- `numberOfPlayers.maxValue`: 2
- `offers.price`: 0
- `genre`: Board Game
- `inLanguage`: zh-CN
- `url`: https://3t.onepis.net/

W3 §JSON-LD payload 抽到 lib/home-jsonld.ts 单一真相 + XSS guard（`<` → `\u003c`）—— 设计合规。

### 3.6 service / transport 分离契约（A9 双兼容基础）

**结论：PASS**

lib/db.ts 顶部 doc comment（line 1-50）落地契约：
- 「service 层纯函数 / 传输层薄壳强制分离」
- 「transport 层零业务规则；任意 transport（RESTful route / GraphQL resolver / gRPC handler）只做「解析入参 → 调 service → 映射返回值到 status code / problem+json」」
- 「不得在 transport 里再写一份「read → mutate → upsert」业务规则」
- 「GraphQL 双兼容仅需新增 schema + resolver，service 函数零改动」

5 service 纯函数：`loadRecordByName` / `upsertRecordByName` / `mergeRecordByName` / `recordOutcomeForName` / `registerOrLoginName` —— 全部零 HTTP 上下文，零 NextResponse 引用。

route handler 仅做：解析入参 → 调 service → 映射返回值到 status code / problem+json —— 4 路由（app/api/sessions + players/[name]/stats/{route,merge/route,outcomes/route}.ts）实测。

### 3.7 探针工艺合规

**结论：PASS**

- 6 active probes 全部 49/0 PASS（offline-mode 9 / offline-result 6 / offline 5 / home-return 11 / one-screen 12 / one-identity 6）
- 2 DISABLED probes（sync-qa + merge-sync-qa）保留为 W2 retire sentinel，header 注释含 coverage migration plan；DISABLED 不视为回归
- `tests/qa/lib/browser.mjs:BASE_URL` env 变量（默认 :3000，QA 用 :3101）—— 无硬编码 URL（F5 fix 已守）
- 新增 one-identity-qa 6 断言实质有效：a) 匿名离线零写零存 / b) 匿名线上不导航 / c) 具名线上 POST outcomes + /result SSR / d) 具名离线 3 局 sessions+merge 各 1 / e) GET 未知 404 problem+json / f) 首页 SSR JSON-LD

### 3.8 六层门禁抽验

**结论：PASS**

§1 表 6/6 全绿。0 fail / 0 regression。commit-audit 7 commit 逐一 PASS（lore trailer 7+ 行/每 commit，Plan: footer 完整）。

---

## 4. 自创对抗矩阵（独立实测）

| # | 对抗用例 | 命令 / 探针 | 实测 | 判定 |
| --- | --- | --- | --- | --- |
| T1 | 旧路由 /solo /play 404 | `curl /solo /play` | 各 404 | ✅ |
| T2 | 旧 API /api/stats×2 /api/solo-stats×3 /api/player-session 全 404 | `curl` 5 旧 URL | 5 路径各 404 | ✅ |
| T3 | 新四端点 URL 全 200 | `curl / /offline /online /result` | 4 路径各 200 | ✅ |
| T4 | POST sessions 幂等（fresh → existed:false + same → existed:true） | `curl POST /api/sessions` x2 | `{existed:false}` → `{existed:true}` | ✅ |
| T5 | GET stats 200 + ghost 404 problem+json | `curl /api/players/{v6test,ghost}/stats` | 200 application/json + 404 application/problem+json | ✅ |
| T6 | POST outcomes 200 累加 + ghost 404 防静默建档 | `curl POST /api/players/{v6test,ghost}/stats/outcomes` | 200 `{stats:{totalGames:1,...}}` + 404 application/problem+json | ✅ |
| T7 | POST merge 200 per-field 加 + ghost 409 player-session-required | `curl POST /api/players/{v6test,ghost}/stats/merge` | 200 per-field 累加 `{totalGames:4,...}` + 409 application/problem+json | ✅ |
| T8 | 422 invalid-player-name (25-char) | `curl POST /api/sessions '{"name":"abcdefghijklmnopqrstuvwxy"}'` | 422 application/problem+json | ✅ |
| T9 | 400 invalid-json (non-JSON body) | `curl POST /api/sessions 'not-json'` | 400 application/problem+json | ✅ |
| T10 | 旧 key ttt.solo.* 生产 src 零出现 | `rg 'ttt\.solo\.' app/ components/ lib/ db/ -t ts -t tsx` | 0 hits | ✅ |
| T11 | 旧 path /solo /play 生产 src 零出现 | `rg "'/solo\b\|'/play\b" app/ components/ lib/ db/ -t ts -t tsx` | 0 hits | ✅ |
| T12 | 旧 api 路径 /api/stats /api/solo-stats /api/player-session 生产 src 零出现 | `rg` 3 path | 0 hits | ✅ |
| T13 | 旧函数 StatsHydrator + 字段 lastWriteAt 生产 src 零出现 | `rg` | 0 hits | ✅ |
| T14 | 展示页双 CTA testid 渲染 | `curl / \| grep data-testid="start-(offline\|online)"` | 2 hits | ✅ |
| T15 | 展示页 JSON-LD 12 字段全 PASS | `curl / \| grep 12 KW` | 12/12 PASS | ✅ |
| T16 | 6 探针全绿 49/0 | `node 6 probe mjs` | 49/0 | ✅ |
| T17 | 文件结构：app/api/* route.ts 仅 4 新端点 | `find app/api -name route.ts` | 4 files | ✅ |
| T18 | 文件结构：app/* page.tsx 仅 4 路由 | `find app -name page.tsx` | 4 files | ✅ |
| T19 | 0 TODO/FIXME/XXX/HACK 在 prod src | `rg "TODO\|FIXME\|XXX\|HACK" app/ components/ lib/ db/ -t ts -t tsx` | 0 hits | ✅ |
| T20 | 0 `as any` / `@ts-ignore` / `@ts-expect-error` 在 prod src | `rg` | 0 hits | ✅ |
| T21 | 0 `console.log/warn/error` 在 prod src | `rg` | 0 hits | ✅ |
| T22 | 7 commit lore trailer 完整（Plan: footer 必备） | `git log -1 --format="%B" \| grep -E "^(Constraint:\|Rejected:\|Confidence:\|Scope-risk:\|Directive:\|Tested:\|Not-tested:\|Plan:)"` | 7/7 commit ≥ 7 lines（含 Plan: footer） | ✅ |
| T23 | :3000 零触达（主公活服） | `lsof -i :3000` | 0 hits（仅 :3101 QA hermetic） | ✅ |

**23 项对抗全 PASS**。

---

## 5. P0 / P1 / P2 分级发现

### P0（必须修复，阻塞合并）
无。

### P1（建议修复，文档契约级）
无。

### P2（建议改进，文案 / 注释 / 测试覆盖级）

**P2-1** · lib/db.ts:38 注释 `sync` 笔误（应为 `merge`）。
- 文件：`lib/db.ts:38`
- 注释：「W2 落地：app/api/players/{name}/stats/{...,outcomes,sync}/route.ts 与」
- 实际：W2 落地实为 `merge/route.ts`（custom method 形态实现为子路径）
- 严重度：P2（文档 typo，不影响运行；rg zero impact on production）
- 修复建议：1 token 改写「outcomes,sync」→「outcomes,merge」即可。
- V5 移交项延续：本次复核仍存在，W5 已知但未改。

**P2-2** · components/ResultBanner.tsx 残留 `/solo` 注释 + 实际成孤儿文件。
- 文件：`components/ResultBanner.tsx`（63 lines）
- 注释 line 7 + 11 含 `/solo` 路径语义描述
- 实际状态：W3 ResultNavigator 接管 + ResultActions 删除后无 import 引用；`rg -l "from.*ResultBanner\|import.*ResultBanner" app/ components/ lib/` = 0 hits
- 仅 `tests/qa/offline-mode-qa.mjs:7,189` 含 comment 提及（描述已退役 banner 形态）
- 严重度：P2（dead code + 注释 stale；不影响运行但增加维护负担）
- 修复建议：W4 一行 `git mv components/ResultBanner.tsx components/_archived/ResultBanner.tsx` 或直接删除（+ README HISTORY 段记录），同步 offline-mode-qa.mjs 注释。
- V5 移交项延续。

**P2-3** · offline-stats-anonymous 提示仅 vitest 断言无 e2e 探针断言。
- 文件：`components/OfflineStatsPanel.test.tsx:59 / :77`（vitest 有）；`tests/qa/*.mjs` 0 hits（探针无）
- testid `data-testid="offline-stats-anonymous"` 已生效（lib/offline-stats.ts + components/OfflineStatsPanel.tsx 双向落地）
- vitest 覆盖：`expect(html).toContain('data-testid="offline-stats-anonymous"')` + `expect(screen.getByTestId('offline-stats-anonymous')).toBeInTheDocument()`
- 严重度：P2（单层覆盖足够，因 UI 元素无副作用 + 不接网络 + 不操作持久层；但 e2e 可补 UX 一致性证据）
- 修复建议：W5 后一波 e2e 探针（offline-qa 或新 one-screen-qa 增量）补 1 step 断言「无名离线进 /offline → offline-stats-anonymous 可见 + 全程 0 /api/* 请求」即可。
- V5 移交项延续。

### 已知缓议（零影响项，已调度者授权）

**K-1** · tests/qa/sync-qa.mjs + tests/qa/merge-sync-qa.mjs DISABLED 桩保留。
- 文件：1564 + 2102 bytes；header 注释含详细 coverage migration plan（W2 + W3 退役原因 + 替代探针映射）
- 状态：DISABLED sentinel，0 run 影响；保留作历史证据
- 严重度：零影响（设计意图）

**K-2** · docs/screenshots/solo.png 文件名沿用旧名。
- 文件：docs/screenshots/solo.png（15733 bytes，1280x900 PNG，内容已 W5 重制为 `/offline` 棋盘）
- 状态：W5 依调度者指令「截图表文件名不变」保留
- 严重度：零影响（README 已核对 board.png ↔ /online、solo.png ↔ /offline 语义）

---

## 6. 章程专查总结

8 项全部 PASS（§3） + 23 项自创对抗全 PASS（§4）+ AC A1-A11 全 PASS（§2）+ 六层全绿（§1） + 5 已知小疵（3 P2 + 2 K-零影响）。

---

## 7. 终判

**ACCEPT-WITH-NOTES**

- 0 P0
- 0 P1
- 3 P2（P2-1 lib/db.ts:38 sync 笔误 + P2-2 ResultBanner.tsx 孤儿 + P2-3 offline-stats-anonymous 无 e2e 探针）
- 2 K-零影响（DISABLED 桩 + solo.png 文件名）
- 六层门禁 6/6 自跑全绿（§1）
- 6 探针 49/0 全绿（§3.7）
- AC A1-A11：11/11 PASS（§2）
- 章程专查 8 项全 PASS（§3）
- 自创对抗 23 项全 PASS（§4）

**理由**：核心契约零回归——A1 退役绝迹（/api/stats×2 /api/solo-stats×3 /api/player-session 7 路径全 404 + rg StatsHydrator/lastWriteAt 0 hits）+ A2 RESTful 四端点全路径（幂等 + 404 problem+json + 409 防静默建档 + 422 + 400 5 路径）+ A3 语义清退（solo/ranked/旧 key/旧 path 0 活引用）+ A4 守卫（online 入口拦截 + offline 无名不记 one-identity-qa step a/b）+ A5 实时（POST outcomes + /result SSR 新数字）+ A6 纯本地（offline 3 局 sessions 1 + merge 1）+ A7 offline-result-qa 六步全绿 + A8 展示页 + JSON-LD 12 字段 + A9 双兼容成文（service/transport 分离契约落 lib/db.ts top-doc + AGENTS.md + README GraphQL 预留节）+ A10 门禁六层 + A11 终验无未处置 P0/P1。代码 correctness 与 e2e 行为完全可控。3 P2 均为文档 / 注释 / 测试覆盖级，零代码阻塞。

**建议处置**：W5' 后一波轻量 commit 落 P2-1（1 token 改写）+ P2-2（git mv 或删）+ P2-3（offline-qa 增 1 step 或新探针）三项修复，独立 commit、独立六层、独立 commit-audit。

**冻结候推**：
- HEAD b0ef6db 含 3 P2（文档 / 注释 / 测试覆盖级）+ 2 K-零影响
- 主公可接受当前候推（W5' 修复项可后续 commit），或要求修复后冻结

---

## 8. 资源自扫 + cleanup receipt

- `/tmp/ulw-og2v/v6.db`：V6 评审 hermetic 库（独立 sqlite，与生产 .env / Turso 隔离）
- `/tmp/ulw-og2v/v6-server.log`：V6 评审 server 启动 + 响应日志
- `/tmp/ulw-og2v/v6-{offlinemode,offlineresult,offlineqa,homereturn,onescreen,oneidentity,offlinemode-rerun}.log`：6 探针 stdout/stderr 落档
- `:3101`：未启（评审末已 `pkill -9 -f "next start -p 3101"` + `lsof -i :3101` 复核为空）
- `:3000`：`lsof -i :3000` 0 命中，主公活服零触达
- 工作树唯一未提交改动：`.gitignore`（主公侧，未卷入本评审）+ `.omo/plans/glossary-context-md.md`（另一待批计划，未追踪，未卷入本评审）
- 本评审仅写 `reports/review/V7.md`（commit-audit 候补 commit 待主公授权后 push）

```
sessions:
  v6 reviewer — fresh codex session, agent_type=explorer-style oracle
  no shared state with execution waves (W1=7765b3f, W2=1fcc41e, W3=c9ef27f, W4=d07fd07, W5=d6eccd3, 调度者注=6bbeac5+b0ef6db)
  cleanup: no live processes, no tmux sessions, no bound ports from V6
```

---

## 9. 与计划 §4 验收 100% 对照

| 计划 §4 AC | 本评审实测 | 状态 |
| --- | --- | --- |
| A1 退役绝迹 | rg 0 hits + 5 旧 API 全 404 | ✅ |
| A2 RESTful 四端点 | SC1-SC5 5 路径 + 4 端点正反全 PASS | ✅ |
| A3 语义清退 | rg solo/ranked/key/path/api 0 活引用 + 12 文件结构正名 | ✅ |
| A4 无名不记 + 入口拦截 | one-identity-qa step a/b | ✅ |
| A5 online 实时 | one-identity-qa step c | ✅ |
| A6 offline 唯一网络写 | one-identity-qa step d | ✅ |
| A7 offline 体验回归（按调度者注六步） | offline-result-qa 6 步 | ✅ |
| A8 展示页 + JSON-LD | curl / 双 CTA + 12 JSON-LD 字段 + 4 路由 200 | ✅ |
| A9 双兼容成文 | lib/db.ts top-doc + AGENTS.md 5 行 + README GraphQL 预留节 | ✅ |
| A10 门禁 + 探针 + commit-audit | 六层全绿 + 49/0 探针 + 272/0 commit-audit | ✅ |
| A11 终验 | 本判：ACCEPT-WITH-NOTES（0 P0 / 0 P1 / 3 P2） | ✅ |

---

**Verdict: ACCEPT-WITH-NOTES**

V7.md ends.
