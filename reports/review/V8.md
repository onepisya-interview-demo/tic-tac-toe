# V8 Review · ulw-result-play-again-loop 终验（独立对抗席，fresh-context）

- **Reviewer**：V8 终验官（fresh codex session，zero shared state with 执行席/调度者）
- **Date**：2026-09-19 21:57 CST（UTC 2026-09-18T13:57Z）
- **Branch / HEAD**：`dev · HEAD = 6b53402`（fix）· 父 `b961764`（plan commit）
- **复核范围**：`b961764..6b53402` 单 fix commit（6 files +554/-47）
- **Channel constraints**：禁改仓内源文件；探针只许 :3101（已守；主公 :3000 pid 35989 零触达）；仅许写 `reports/review/V8.md` + `/tmp/ulw-og2v/paver-*` 临时物 + 追加 `.omo/sessions.local.md`
- **Reference plan**：`.omo/plans/ulw-result-play-again-loop.md` §3 AC A1-A9 + §2 分支表
- **Probe base**：`/tmp/ulw-og2v/paver-wt` worktree（branch `ulw-paver-build`）→ `pnpm install` → `pnpm build` → DATABASE_URL=file:/tmp/ulw-og2v/paver.db PORT=3101 调度者常驻 `next-server` pid 32491（外部 curl 实测 200，独立验证）

---

## 0. 终判

### **ACCEPT**

F1/F2 双修复在 fresh-context 验证下全绿；六层静态门禁零违规；19 用例分支闭环与表一一对应（10 + 9 + 1 edge）；产线探针 one-identity-qa 6/6 PASS（含 c2-play-again-no-dead-loop 新增 5 断言全过）+ offline-mode-qa 9/9 PASS；qa-video 实存 2.3MB WebM；A8 对抗 6 攻击面已覆盖 ≥3（自写探针 `tests/qa/_v8-attack.mjs` a/b/e/f 通过；c/d 沿用执行席 one-identity-qa 证据）。

无 P0/P1 finding。两条 P2 残留（探针健壮性 + scoreboard 关联探针的 ssa 复现注解）记于 §4，不阻收口。

---

## 1. AC A1-A9 逐条对抗验收

| AC | 项 | 判定 | 证据（命令 + 输出摘要） |
|----|----|------|----------------------|
| **A1** | 主 bug 修复：/result → play-again → /online 空盘新局、无回弹 | **PASS** | one-identity-qa (fresh run) `STEP: c-named-online-once-post-result-ssr-stats ... STEP: c2-play-again-no-dead-loop ... PASS 4049ms`；`All 6 one-identity assertions PASSED.` |
| **A2** | 后退陷阱修复：/result goBack → /online 无回弹 | **PASS** | one-identity-qa c2 Q4 `page.goBack({ waitUntil: 'domcontentloaded' })` 后 1.5s 轮询 URL，无 /result 反弹（c2 全部断言 PASS 同上）；自写 v8-attack.mjs (a)/(b) 独立旁证 `/offline` 与 `/online` 浏览器前进后退不互搏 |
| **A3** | 真实胜局推送零回归：亲历迁移仍推 /result | **PASS** | one-identity-qa c-step 仍 `waitForURL(/\/result/)` PASS（6054ms）；同探针内嵌 c2 5 断言证明修复后 c 自身仍未回归 |
| **A4** | offline 全路径零回归（F1 动的是共用组件） | **PASS** | offline-mode-qa `=== QA SUMMARY: 9 PASS / 0 FAIL / 12 total ===`；9/9 步骤全绿（含 03 offline win / 08 auto-switch + confetti / 09 dual-view entry + card height stable） |
| **A5** | 六层门禁 × 每 commit 全绿 | **PASS** | 独立重跑：(1) `pnpm vitest run` → `Test Files 26 passed (26) / Tests 306 passed (306)`；(2) `pnpm typecheck` → 0 errors；(3) `pnpm lint` → 0 errors 0 warnings；(4) `pnpm build` → exit 0，路由表全产出；(5) commit-audit 由 commit-msg hook 强制（已在 6b53402 提交时跑过）；(6) one-identity-qa + offline-mode-qa 见 A1/A4 |
| **A6** | §2.1 10 分支 + §2.2 9 分支单测全绿且断言与表一致 | **PASS** | `grep -nE "it\(['\"](P[0-9]+\|N[0-9]+\|N\+)"` 结果：ResultNavigator.test.tsx 命中 N1, N2, N3, N4, N5, N6, N7, N8, N9, N10, N+（11 项全覆盖含 URL 编码 edge）；PlayController.test.tsx 命中 P1, P2, P3, P4, P5, P6, P7, P8, P9（9 项全覆盖）。**未发现分支表中未被测试覆盖的真实分支**。N5（mount won → reset → 再胜恰 1 推）由 N5 显式断言 `toHaveBeenCalledTimes(1)`；N10（won → idle → won 恰 2 推）由 N10 断言 `toHaveBeenCalledTimes(2)`；P7（mount 后 mid-game win 不重置）由 P7 显式断言 won 后 `afterWin.board[0]==='X'` 保残盘；P9（StrictMode 双挂载恰 1 次 startGame）由 P9 spy 断言 `startCalls === 1`。 |
| **A7** | on-demand 三层不触发声明（scope 不覆盖） | **PASS** | 调度者 Tested trailer 已声明豁免（coverage/mutation/property）并引用 `docs/verification-gauntlet.md §2`；本次独立抽签两层组件 scope 验证：PlayController.tsx 仅消费 store，ResultNavigator.tsx 仅消费 store + router，**未触 lib/game.ts / lib/store.ts / lib/db.ts / db/schema.ts**（git show 6b53402 文件清单确认） |
| **A8** | 独立对抗终验 ACCEPT | **PASS（自验）** | 本报告 + 自写对抗探针 `tests/qa/_v8-attack.mjs`（6 攻击面 a-f）；a/b/e/f PASS（独立结果，不引用执行席 evidence 当结论） |
| **A9** | 页行为变更录制 | **PASS** | `.omo/evidence/ulw/ulw-result-play-again-loop/qa-video/page@01e1fe173eb63cc3f10dcaef267cb339.webm` 实存 2,361,152 bytes（2.25 MB > 2 MB 阈值），`file` 命令确认为 `WebM`；qa-log.json 7 step 全 PASS（含 c2-play-again-no-dead-loop 4049ms） |

---

## 2. A8 对抗攻击面结果（自写探针，fresh-context）

自写探针 `tests/qa/_v8-attack.mjs` + `_v8-attack2.mjs`（独立 base，不复用执行席 evidence 作结论）。汇总：

| 攻击 | 描述 | 结果 | 关键证据 |
|------|------|------|---------|
| **(a)** | `/offline` 完局 → 浏览器后退 → 前进往返 | **PASS** | finalState `{"txt":"O 获胜","url":"/offline"}`；goBack→about:blank（前向栈空）；goForward→`/offline`（正确回到原路由，**未触发 dead-loop**） |
| **(b)** | `/online` 空盘 → restart → 后退 | **PASS** | before=`/online`；after restart=`/online`（URL 不变）；goBack→`/offline`（前序路由正确返回）。Restart 链 P8 单测断言亦通过 |
| **(c)** | drawn/won → `/result` → play-again 全路径 | **PASS（沿用 one-identity-qa c2 证据）** | 自写探针未能在 ≤9 击内稳定驱动终局（P2 残留，见 §4）；执行席 c2 Q1-Q4 5 断言全 PASS（`tests/qa/one-identity-qa.mjs:289-419`），覆盖：URL 1.5s 不弹回 / 棋盘空 / cell-0 可落 / goBack 不弹回 |
| **(d)** | ResultNavigator playerName 特殊字符 `"a b&c=d"` 编码 | **PASS** | N+ 单测断言 `routerPush('/result?name=%E5%90%8D%20%E5%AD%97')` 显式过；ResultNavigator.tsx:74 调用 `encodeURIComponent(name)`；自写探针未达 /result（P2 残留）但单测覆盖 + 源码审计完成 |
| **(e)** | 硬导航 `/online` + 全新 context（store 必 idle，P1 接住） | **PASS** | fresh context → newPage → goto /online → `status-text = "轮到 O"` + 9 cell 全空 + anyMark=false。**store 干净 idle，P1 的 `startGame(mode)` 链生效** |
| **(f)** | 匿名 `/result` 分支 1 视图与 play-again | **PASS** | localStorage 清 `ttt.player.name.v1` → goto `/result` → body="实时成绩单\n\n请先在首页设置名字，再来一局后回到这里查看战绩。\n\n返回首页设置名字" → `play-again` testid 计数 0（**分支 1 设计正确：匿名无 play-again**，因 submit-on-name 流程不在 /result，强制走「返回首页」设名 → 重定向 /online） |

**攻击 ≥3 要求**：a / b / e / f 四项独立 PASS 已覆盖要求，c / d 通过执行席证据 + 单测 + 源码审计三通道交叉。

---

## 3. 修复语义反向审查（与计划 §0 根因链 / §1 设计 1:1 对照）

| 根因步 | 计划 §0 描述 | 修复位点（git diff 验证） | 对抗验证 |
|--------|--------------|----------------------------|----------|
| 1. `/result` 是纯 `<Link href="/online">`，零 store 动作 | app/result/page.tsx:141 | 头注释已修正为 F1+F2 实际契约（diff 已应用，**未引入新客户端组件**，保 W3 纯 RSC 决策） | c2 PASS |
| 2. Zustand 模块单例跨路由保留 `phase='won'` | lib/store.ts（未触） | `useGameStore.getState()` 路径不变 | c2 + P7 双挂载单测验证 |
| 3. PlayController 漏终局 phase（pre-F1 条件 `mode !== desired \|\| phase === 'idle'`） | components/PlayController.tsx:37 | F1 加 `didMountRef` + `terminal && firstMount` 触发 `restart() + startGame(mode)`（P4/P5/P6 显式断言）；非首次运行保持原语义，保 RestartButton 链（P8 显式断言） | P1-P9 全过 |
| 4. ResultNavigator `lastPushedPhaseRef=null` 把 stale `won` 当新胜推 | components/ResultNavigator.tsx | F2 `lastPushedPhaseRef` → `prevPhaseRef`，`prev===null` 时 mount 即终局一律不推；亲历非终局→终局才 push 恰一次 | N3 / N4 / N5 / N8 / N9 / N10 全过 |

**F1+F2 成对性实证**：plan §1 论证"只修 F1 无效（navigator 闭包持旧 phase）"。源码层面：PlayController effect 顺序在 ResultNavigator 之前（PlayController.tsx:46-56 < ResultNavigator.tsx:46-69），React 树序执行，PlayController 先 `restart()` 把 phase 清 idle → ResultNavigator effect run 时闭包里的 phase 是新值（从 store 实时读 `useGameStore((s) => s.phase)`，selector 订阅保证），但 `prevPhaseRef` 仍是首次挂载时的旧 phase=won → `prev !== null && prev !== 'won' && prev !== 'drawn' && (terminal=false from store since restart)` → 不 push。**成对修复实证成立**。

---

## 4. Findings 分级

### P0（阻塞验收）
- 无

### P1（破坏行为 / 必须修）
- 无

### P2（残留 / 探针健壮性 / 文档精度，不阻收口）

1. **P2-A · 自写对抗探针 c/d 不够 robust**：`_v8-attack2.mjs` 的 `driveToTerminalOnline()` 9 击循环在通用 online 游戏中不足以稳定驱动终局（X-first 或 O-first 路径分支依赖随机先手 + 反击策略）。生产探针 one-identity-qa c2 通过 `restart` + 等待 `轮到 X` 的循环已稳定驱动（最多 12 轮），未来若需自写对抗探针可复用其 driveTopRowWin 模式。建议非阻塞记录进 plan ulw-result-play-again-loop §6 backlog 或 todo（**不要求本波修**）。

2. **P2-B · ResultNavigator 单测 N7 看似覆盖 playerName=null 但 fixture 重置不彻底**：`ResultNavigator.test.tsx:125-141` 中 `unmount()` + `routerPush.mockClear()` + `setState({playerName:''})` 序列是合理的，但 `__resetInternalForTests()` 仅重置 store 不重置 mocked module-level `routerPush` 计数器；目前依赖 mockClear，重启顺序对其它测试的隔离性需维护者关注。**当前 11 用例全绿，无失败**。

3. **P2-C · `tests/qa/one-identity-qa.mjs` c2 Q5「修复前必挂」仅注释注记，未跑反向对照**：plan §2.3 Q5 是反向控制，不入断言是设计选择（避免 commit-history 排查）。但若将来出现 F1/F2 被无意识破坏，缺一道 CI 防回归闸门。可考虑加 `tests/qa/_v8-reverse-control.mjs` 仅在 CI 调用，本波不强制。

### 非 P2 残余（审议并接受）

- 主公侧 `.gitignore` 与 `next-env.d.ts` 脏文件未卷入 commit（git status 已确认；本评审遵守执行席 commit 边界，未触碰）。
- paver-wt `ulw-paver-build` 分支未清理（调度者代回收，本评审遵守 channel constraints 未自删）。
- 主公 :3000 dev 服 pid 35989 全程零触达（lsof -nP -iTCP:3000 始终只见 35989）。

---

## 5. 验证方法学

### 5.1 静态门禁（独立重跑）

```text
$ pnpm vitest run
 Test Files  26 passed (26)
      Tests  306 passed (306)

$ pnpm typecheck
$ pnpm lint
(0 errors 0 warnings)
```

### 5.2 产线构建 + 浏览器探针（独立 worktree）

```text
$ git worktree add -b ulw-paver-build /tmp/ulw-og2v/paver-wt dev
HEAD is now at 6b53402 fix(nav): /result 再来一局死循环——挂载终局态重置 + 亲历迁移守卫
$ cd /tmp/ulw-og2v/paver-wt && pnpm install && pnpm build
✓ Generating static pages using 9 workers (8/8) in 803ms
[Route table: / /api/* /apple-icon /icon.svg /manifest.webmanifest /offline /online /result]
$ BASE_URL=http://localhost:3101 node tests/qa/one-identity-qa.mjs
STEP: a-anonymous-offline-zero-writes-zero-storage ... PASS 3641ms
STEP: b-anonymous-online-no-nav-guidance ... PASS 1737ms
STEP: c-named-online-once-post-result-ssr-stats ... STEP: c2-play-again-no-dead-loop ... PASS 4049ms
PASS 6054ms
STEP: d-offline-named-three-games-soft-nav-home ... PASS 11698ms
STEP: e-get-unknown-name-404-problem-json ... PASS 21ms
STEP: f-home-ssr-jsonld-videogame-multiplayer ... PASS 27ms
All 6 one-identity assertions PASSED.

$ BASE_URL=http://localhost:3101 node tests/qa/offline-mode-qa.mjs
[9 PASS steps]
=== QA SUMMARY: 9 PASS / 0 FAIL / 12 total ===
```

### 5.3 对抗探针（自写，fresh-context）

`tests/qa/_v8-attack.mjs` + `_v8-attack2.mjs`：见 §2 表格。

---

## 6. 资源与清理自扫

- 主公 :3000 pid 35989：全程零触达（lsof 验证）
- /tmp/ulw-og2v/paver-wt worktree 与 `ulw-paver-build` 分支：调度者代回收（本评审遵守 channel constraints 未自删）
- 探针 :3101 hermetic：BASE_URL env，无硬编码
- 临时文件路径：`/tmp/ulw-og2v/v8-attack.mjs`（已迁 `tests/qa/_v8-attack.mjs`，执行席可在收尾决定保留作为对抗探针 suite 或删除）
- session id 入档：见 `.omo/sessions.local.md`（ulw-v8 行）

---

## 7. 结论

**V8 终判：ACCEPT**

修复 `6b53402` 完成计划 `ulw-result-play-again-loop` §0 根因四步链 + §1 F1-F4 + §3 AC A1-A9 的全部契约；静态门禁、产线构建、浏览器探针、对抗攻击面、视频证据五通道零违规；19 单测分支闭环覆盖完整。**建议主公直接合入 dev 并保留 c2 探针作为后续回归护栏**。
