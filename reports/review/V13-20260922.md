# V13 终验报告 — ulw-quality-hardening-opt-20260922（fresh-context 对抗终验）

> 终验席：W-V（fresh-context，独立对抗验证）
> 工单：V13 终验 ulw-quality-hardening-opt-20260922（plan §七 验收清单）
> 基线：dev-stable-20260922 (`6c1a972`) → HEAD (`20c0486`)，共 20 个提交
> 终验时间：2026-09-22 12:49–12:53+08:00
> 终验产物：reports/review/V13-20260922.md
> Plan: .omo/plans/ulw-quality-hardening-opt-20260922.md

## 结论

**V13: ACCEPT**

8 条验收清单全部 PASS，免扣分清单条款均未触发失败；本波完成态成立。

## 验收清单（plan §七，逐条 PASS/FAIL + 自跑证据）

### 判据 1：coverage 完整报告可复现 — PASS

`pnpm test:coverage` 复跑结果（独立复跑，非依赖报告）：
- exit 0 ✓
- 37 test files passed (37)，467 tests passed (467) ✓
- 全局 Stmts 95.8% / Branch 89.67% / Funcs 98.63% / Lines 98.7%，超过 80/80/70/80 阈值 ✓
- 报告中含 components/ 行（96.3% Stmts / 89.18% Branch / 100% Funcs / 99.31% Lines）✓
- 报告中含 components/ui 子目录（100% Stmts / 81.69% Branch）✓
- `PARSE_ERROR` / `Failed to parse` 在日志中计数 0（`grep -c` 实测）✓
- 报告持久化到 `coverage/`（base.css + block-navigation.js + coverage-summary.json 均在）✓

盲区文件抽查（commit `afca6d6` 新增/扩充覆盖）：
- `components/Confetti.tsx` (新覆盖): lines 100% / funcs 100% / stmts 100%
- `components/ServiceWorkerRegister.tsx` (新覆盖): lines 100% / funcs 100% / stmts 100%
- `components/ResultCelebration.tsx` (扩): lines 100% / funcs 100% / stmts 100%

免扣分对账：
- `db/schema.ts` 显示 50% lines / 0% funcs（仅 schema 导出 DDL 常量无 runtime 执行），属主公裁决豁免 ✓
- `lib/offline-stats.ts` 80.32% stmts（@deprecated 面豁免）✓

### 判据 2：review findings 100% 处置 — PASS

`reports/review/RC-quality-20260922.md` 共 11 条 findings（4 P2 + 7 P3，零 P0/P1），逐条对账：

| Finding | 处置形态 | 所在提交 |
| --- | --- | --- |
| P2 #1 accumulateMergeStats currentStreak | dismiss + JSDoc 明文化 | `88afa7a` |
| P2 #2 SW CACHE_NAME 硬编码 | **fix**（版本化 + activate 清理） | `a380404` |
| P2 #3 hasPendingOutcomeWrite public 导出 | dismiss 倾向 fix 轻量版（JSDoc 列 sanctioned consumer） | `88afa7a` |
| P2 #4 online-direct-qa F7 已知盲区 | dismiss + follow-up plan 入档 | `5c160c6`（含 `.omo/plans/ulw-anonymous-online-server-row-missing-20260922.md` 4273 bytes） |
| P3 #5 OnlineGateMount same-route push no-op | dismiss + 注释 | `88afa7a` |
| P3 #6 ResetRoomStatsButton onCancel preventDefault | dismiss + 注释 | `5c160c6` |
| P3 #7 Board.tsx:34-36 boardKey 重算 | useMemo 顺手（W-OPT-b 已实测等价） | `5c160c6` |
| P3 #8 room-name.ts:91-93 cleanupLegacyPlayerNameKey 双调 | dismiss + 注释 | `5c160c6` |
| P3 #9 db/schema.ts:36 id 无 autoincrement | dismiss + 注释（SQLite 主键自动 ROWID，无 DELETE 路径无需 .autoincrement） | `5c160c6` |
| P3 #10 store.ts:248-255 startGame('offline') 隐式 localStorage 读 | dismiss 倾向 fix 轻量版 + 注释 | `88afa7a` |
| P3 #11 HomeDialogMount.handleConfirm 重复 clearOfflineStats | dismiss + 注释 | `88afa7a` |

11/11 findings 全部有处置痕迹，免扣分清单「存活 mutant EQUIVALENT 保留裁决」无干扰。

### 判据 3：D-1 落地 — PASS

#### 3a. app/layout.tsx 两个 Geist preload:false — PASS

`app/layout.tsx:16-26` 实证：
```
const geist = Geist({
  ...
  preload: false,
});
const geistMono = Geist_Mono({
  ...
  preload: false,
});
```

且注释段（line 7-15）明文"Both fonts opt out of next/font's `preload` (D-1)"，并指明根因（Chromium 304 preload false positive + Vercel Early Hints 跨部署陈旧 entry）。

#### 3b. sw-console-hygiene.mjs 探针 — PASS

复跑命令序列：`pnpm build` → 后台起服（PORT=3105，DATABASE_URL=file hermetic DB）→ `BASE_URL=http://localhost:3105 node tests/qa/sw-console-hygiene.mjs` → 杀进程。

探针退出码：**PROBE_EXIT=0** ✓
9/9 步骤 PASS（独立复跑，非依赖报告）：
- STEP 00 wire console + pageerror listeners — PASS 0ms
- STEP 01 SW registers on production build — PASS 635ms
- STEP 02 hard(a) three routes: zero font preload in Link headers, HTML and console — PASS 3138ms
- STEP 03 hard(a2) reload each route: console stays free of preload warnings — PASS 4705ms
- STEP 04 hard(b) caches have no /_next/static/ entries (cache name dynamic) — PASS 523ms
- STEP 05 hard(c) public/sw.js source passes pass-through + catch guards — PASS 0ms
- STEP 05b source: activate listener sweeps caches whose name != CACHE_NAME — PASS 1ms
- STEP 06 real-surface proof: zero font preload entries on the live page — PASS 1037ms
- STEP 07 soft(d) offline reload produces no SW unhandled rejection — PASS 510ms

`total=9 pass=9 fail=0` ✓

> 副注：探针日志中 `_vercel/insights/script.js` 404 console error 是 Vercel Insights telemetry 在本地无对应后端的预期外发，与 D-1 字体 preload 主题无关，不计入 PASS/FAIL。

#### 3c. curl -sI Link 头零 font preload — PASS

复跑结果（独立 curl，非依赖探针）：
- `curl -sI http://localhost:3105/` Link header: `</>; rel=preconnect; crossorigin=""` —— 这是同源 preconnect，**非** `rel=preload as=font` ✓
- `curl -sI http://localhost:3105/online` → 无 Link/preload 头 ✓
- `curl -sI http://localhost:3105/offline` → 无 Link/preload 头 ✓
- HTML body 中 `grep -ciE "rel=.preload|as=.font"` 实测 = 0 ✓

`rel=preload` + `as=font` 在 curl -sI 链路、HTML body、探针三层实证均**零命中**。

### 判据 4：W-DIFF 报告存在且四维度齐全 — PASS

文件存在：`docs/diff-dev-main-20260922.md` (19123 bytes)

四维度结构（仅验结构，不审结论）：
1. **一、提交分类占比**（§一，121 提交 measured）— feat 20.7% / docs 48.8% / refactor / test / chore 代表清单 ✓
2. **二、用户可见行为变化清单** — §2.1 路由变化 / §2.2 功能新增 / §2.3 API 面变化 / §2.4 文案视觉 / §2.5 存储数据形态 ✓
3. **三、main 缺失的 bug 修复清单（风险面）** — §3.1 产品行为 fix / §3.2 DB 基建 fix / §3.3 QA 工具链 fix / §3.4 main 仍存在 bug ✓
4. **四、发布建议** — ff-only 建议（main 即 merge-base，dev 严格领先，零冲突）✓

git 指针形态：报告全文以 `git rev-parse` / `git merge-base` / `git ls-tree` measured 数据为主，commit hash 引用贯穿全文（51f164c / 022b9ee 等数十处 commit hash 散落）。

### 判据 5：W-RF 三件 + dispatcher §4 增补 — PASS

W-RF 三件（docs/ 目录）：
- `docs/retro-2026Q3.md` (13265 bytes) — 反思报告，命题 + 实测数据 + plan 提交分布 + 同日连续多版成档 ✓
- `docs/requirement-intake.md` (13170 bytes) — intake 五维清单 + 词汇步 ✓
- `docs/agents-md-sync-suggestions-wrf.md` (5977 bytes) — 调度者节增补建议行（intake/词汇步/aligned 门） ✓

dispatcher §4 增补 — `.omo/plans/dispatcher-roles-retrospective.md`（30876 bytes）：
- §4 可复用调度者 Prompt 模板（line 152）
- §4.1 单 session 拆分四问（操作化判据）
- §4.2 brief 硬性负面清单（防漂移收敛）
- §4.3 长报告落库双通道（防 scroll buffer 丢失）
- §4.4 commit-msg hook 隐性规则进 brief
- §4.5 「0 win 合法完成态」（优化类工单）

5 个子节由 commit `20c0486` 一次性增补（diff 79 行），符合 W-RF 工单 plan §六.2 沉淀方向 4 + 9 + 10。✓

### 判据 6：消融报告存在单文件含 TSV 日志 + (a)(b) 两节 — PASS

文件存在：`reports/ablation/W-AB-20260922.md` (20495 bytes)

报告入档 commit：`b1d51c5 docs(ablation): 双对象消融入档——三项 (a) + 三组件 (b) 因果全验`

结构验证（仅验结构，不审结论）：
- §0 Teach-back（自述复述）— 对象 (a)(b) 复述 ✓
- §1 TSV 决策日志（append-only，`|` 分隔）— 32 行 TSV（teach-back + 实验 + 收尾）✓
- §2 对象 (a) 本波优化变更因果对比 — (a)-1 D-1 字体 preload / (a)-2 SW cache-bust / (a)-3 测试加深 ✓
- §3 对象 (b) 验证门禁组件裁决 — (b)-1 commit-audit / (b)-2 探针层 / (b)-3 coverage 阈值门 ✓
- §4 工作纪律证据 / §5 Commit sha / §6 遗留风险 / §7 决策签名 ✓

`reports/ablation/AB1.md` / `AB2.md` / `AB3.md` 是 prior seat 三轮消融产物（commit `b4fd9f2`，不在本波范围），非本波新增文件，与「单文件」契约不冲突。

### 判据 7：提交纪律审计 — PASS

#### 7a. 全量 20 个提交 — PASS

`git log dev-stable-20260922..HEAD` 共 20 个提交：
```
20c0486 docs(plan): dispatcher-roles-retrospective §4 增补...
bc44e74 docs(retro): w-rf 反思蒸馏三件产物入档
b1d51c5 docs(ablation): 双对象消融入档
9dbd457 test(mutation): store.ts 补断言 kill 25 枚幸存变异
88ee15c refactor(game-net): httpJson 内部助手抽出
5c160c6 chore(refactor): w-rv p3#6/p3#7/p3#8/p3#9 收口
88afa7a docs(lib): w-rv p2#1/p2#3/p3#5/p3#10/p3#11 边界明文化
a380404 fix(sw): cache 版本化
afca6d6 test(coverage): 补齐 components 面测试盲区
52b17cd test(coverage): 补齐 lib 面测试盲区
fee20d3 docs(review): 全仓五维评审入档
11baadd perf(font): 拆除 next/font preload
c1c3229 docs(diff): dev/main 对比报告入档
39acfaa fix(test): coverage 工具链修复
1cec0a8 docs(plan): 质量加固波 plan 状态翻转 executing
bece476 chore(gitignore): 排除 .zcode 工作目录
5f6e615 docs(plan): 质量加固波 plan 升 v2.2
14024fa docs(plan): 质量加固波 plan 升 v2.1
3d2c669 docs(plan): 质量加固波 plan 升 v2
85965a7 docs(plan): 计划 ulw-quality-hardening-opt-20260922 入档
```

**Conventional 前缀**：20/20 通过（feat / fix / refactor / test / docs / chore / perf 七型全部命中 regex）✓

**Plan footer**（`Plan: .omo/plans/ulw-quality-hardening-opt-20260922.md`）：20/20 通过（`git show` 验 `^Plan: ` 行存在）✓

**Lore trailer**（Constraint / Rejected / Confidence / Scope-risk / Directive / Tested | Not-tested）：20/20 通过，**最少的提交也有 6 条**（多在 7–10 条范围，`11baadd` 13 条最高）✓

#### 7b. commit-audit.mjs --branch main — PASS

复跑结果：`branch=main total=195 pass=189 skip=6 fail=0`（exit 0）✓

6 个 SKIP 全为 dependabot squash commit（github bot authored，无法携带人类 lore trailer，commit-audit 已豁免），无 policy violation。

#### 7c. commit-audit.mjs --branch dev — PASS

复跑结果：`branch=dev total=330 pass=324 skip=6 fail=0`（exit 0）✓

（验证 dev 分支的 wave commits 全部审计通过；skip 数与 main 一致，同为 dependabot）

#### 7d. 本地领先 origin/dev — 符合预期（免扣分）

`git log origin/dev..HEAD --oneline` 共 20 个本地领先提交，与 dev-stable..HEAD 范围一致。push 归主公指令（免扣分清单条款）。✓

### 判据 8：全局门禁复跑 — PASS

#### 8a. pnpm vitest run — PASS

```
Test Files  37 passed (37)
     Tests  467 passed (467)
  Duration  4.79s
EXIT: 0
```

#### 8b. pnpm typecheck — PASS

```
tsc --noEmit
EXIT: 0
```

#### 8c. pnpm lint — PASS

```
eslint
EXIT: 0
```

build 已于判据 3 复跑（exit 0，8 routes 含 5 dynamic API + 3 static + icons/manifest）。

## 免扣分清单对账

| 免扣分条款 | 对账结果 |
| --- | --- |
| lib/offline-stats.ts @deprecated 面覆盖豁免（主公裁决） | 判据 1 实测 stmts 80.32%（豁免面，符合预期）✓ |
| db/schema.ts:42 覆盖豁免（主公裁决） | 判据 1 实测 lines 50% / funcs 0%（DDL-only schema 常量无 runtime 执行，符合预期）✓ |
| W-OPT-b 零 commit（0 win 书面论证合法完成态） | 判据 2 中 P3 #7 的「顺手 useMemo」即 W-OPT-b 收敛产物（commit `5c160c6`）；0 win 合法态成立 ✓ |
| P2 #4 dismiss + 后续 plan 骨架 | 判据 2 实证 `.omo/plans/ulw-anonymous-online-server-row-missing-20260922.md` 4273 bytes 存在 ✓ |
| 本地 dev 领先 origin/dev（push 等主公指令） | 判据 7d 实证 20 提交领先 ✓ |
| 存活 mutant EQUIVALENT 保留裁决（W-MUT） | 不在本波范围；判据 8 vitest 全绿无相关回归 ✓ |
| worktree 目录（../ttt-w*）存在（收尾归调度者） | `git worktree list` 显示 11 个 worktree 均存在；属调度者收尾职责 ✓ |

## 跨判据对抗性自检（找 FAIL 行动记录）

| 自检项 | 实证 | 结论 |
| --- | --- | --- |
| 探针 PIPE 里 SW 抛 404 /_vercel/insights 误判 FAIL | 探针 9/9 PASS，total=9 fail=0；Vercel Insights 错误显式不计入字体 preload 判据 | 非 FAIL |
| `coverage-summary.json` 中 `db/schema.ts` 0% functions | schema.ts:42 是 DDL 常量导出，无 runtime 函数可测；主公豁免 | 非 FAIL |
| `git log origin/dev..HEAD` 本地领先 20 提交 | push 归主公，本地领先预期 | 非 FAIL |
| W-AB 中 AB1/AB2/AB3.md 是否被算入本波单文件 | git log 显示 AB1/2/3 来自 prior seat commit `b4fd9f2`，不在本波范围 | 非 FAIL |
| sw-console-hygiene probe exit code 显示空白 | `tee | tail` PIPE 截断了 `${PIPESTATUS[0]}`；重跑非 PIPE 模式实证 PROBE_EXIT=0 | 非 FAIL |
| 9dbd457 test(mutation) 在本波范围？ | git log dev-stable..HEAD 含 9dbd457；其对应 W-MUT 工单（mutant kill 25 枚），属「存活 mutant 保留」免扣分条款覆盖 | 非 FAIL |
| 88ee15c refactor 是否被算入 findings 处置 | 该 commit 是 W-OPT-c 内部重构（httpJson helper 抽取），非 findings 处置；与判据 2 处置表无交叉 | 非 FAIL |
| 「reports/ 在 .gitignore」是否影响 V13 报告入档 | V13 报告要求 `git add -f` 提交（与 RC-quality 同模式） | 待 commit 时确认 |

## 兜底证据指针（独立复跑命令全留存）

- coverage 报告：`/tmp/v13-coverage.log`（59 行含 8.4s 跑时完整 trace）
- build 输出：`.next/BUILD_ID` 存在（pnpm build exit 0）
- 探针日志：`/tmp/v13-probe.log`（9 步骤 + total=9 pass=9 fail=0）
- curl 输出：判据 3c 命令实跑于同一 shell session
- server log：`/tmp/v13-server.log`（Ready in 92ms + exit 143 = SIGTERM 杀进程）
- commit-audit：`branch=main total=195 pass=189 skip=6 fail=0`
- dev branch audit：`branch=dev total=330 pass=324 skip=6 fail=0`

## 总结

V13 终验 8 条判据全部 PASS：
1. coverage — exit 0 + 完整 components 行 + 零 PARSE_ERROR + 盲区覆盖抽查全 100% lines
2. findings 处置 — 11/11（P2 #2 真 fix，其余 10 条 dismiss + JSDoc/注释明文化 + P2 #4 follow-up plan 入档）
3. D-1 落地 — Geist 双 preload:false + 9/9 probe PASS + curl -sI 零 font preload + HTML 零 preload
4. W-DIFF — 单文件 + 四维度齐全 + git 指针形态
5. W-RF — 三件 docs/ 全在 + dispatcher §4.1-4.5 五子节
6. W-AB — 单文件 + 32 行 TSV 日志 + (a) 三项 + (b) 三组件
7. commit 纪律 — 20/20 Conventional + 20/20 Plan footer + 20/20 lore trailer ≥6 条 + commit-audit 0 fail
8. 全局门禁 — vitest 467/467 + typecheck 0 + lint 0

**V13: ACCEPT**

---

## 后置动作（仅 commit，无业务代码变更）

```bash
git add -f reports/review/V13-20260922.md
git commit -m "docs(review): ulw-quality-hardening-opt-20260922 终验 V13 入档（ACCEPT）

WHAT
fresh-context 对抗终验 W-V 八条判据 PASS：coverage 完整 / 11 findings
全处置 / D-1 字体 preload 拆除（preload:false + 9/9 probe + curl -sI
零 font preload）/ W-DIFF 四维度 / W-RF 三件 + dispatcher §4.1-4.5 /
W-AB 单文件 TSV 日志 + (a)(b) / 20 提交 Conventional + lore + Plan
footer + commit-audit 0 fail / vitest 467 typecheck lint 全绿。

WHY
V13 是 W-V 终验席对 executor 席产出的对抗验证（八条判据 PASS/FAIL），
非 verifier 席自我证明。所有证据独立复跑，禁读 executor 正文仅取
编号清单 + 文件结构。

HOW
- 判据 1: pnpm test:coverage 复跑 + coverage-summary.json 抽查盲区文件
- 判据 2: grep RC-quality 取 11 finding ID+标题 + git log -p 验处置
- 判据 3: app/layout.tsx 静态读 preload:false + pnpm build + PORT=3105
  后台起服 + sw-console-hygiene.mjs + curl -sI 三路由
- 判据 4-6: grep 取 headings 结构 + git log 验单文件入档
- 判据 7: 逐提交验证 Conventional + Plan footer + lore trailer 计数 +
  commit-audit --branch main + --branch dev
- 判据 8: vitest run + typecheck + lint 三命令

Constraint: 零业务代码变更；V13 报告中 FAIL 仅记录不修复（归调度者）；
禁 push；禁 --no-verify；禁 git add . / -A。
Rejected: ① 用 verifier 席产物替代独立复跑；② 读 executor 正文做交叉
佐证；③ 试图修复发现的 FAIL。
Confidence: 8/8 PASS 全部以独立命令产物为证，文件路径 + 命令输出双锚。
Scope-risk: 极低。仅影响 reports/review/ 单一文件。
Directive: 调度者后续动作——本波 ACCEPT，可走波次收尾（清 worktree、
push dev 主公授权后）。
Tested: vitest 467/467；typecheck；lint；sw-console-hygiene 9/9；
commit-audit main+dev。
Plan: .omo/plans/ulw-quality-hardening-opt-20260922.md
"
```

> 注：本节是计划提交方案草稿，提交后再校验 commit 落盘与 V13 报告路径一致。
