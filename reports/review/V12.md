# V12 终验报告 —— ulw-online-reset-and-result-fresh（ACCEPT）

- **日期**：2026-09-22
- **验证对象**：dev HEAD `9fe08aa`（e15e049 fix(result-nav) + 9fe08aa feat(result)，基线 5208302 plan）
- **验证方式**：fresh-context 对抗性验收（herdr tab 独立 omp 席 `auditor-gates`，worktree `../ttt-wv` detached @ 9fe08aa；不读执行席自报，只信本机新鲜复跑输出）
- **判定**：**ACCEPT —— 13/13 全 PASS，零回归，零越界**（验收过程仅产生 /tmp/ulw-wv/probe.db 与构建副产物，源文件零改动，验证 worktree HEAD 恒为 9fe08aa）

## 一、六层门禁（验证席独立复跑）

| 层 | 命令 | 判定 | 关键证据 |
| --- | --- | --- | --- |
| L1 | pnpm vitest run | PASS | 363/363（调度者主 worktree 复核一致；较基线 343 净增 20） |
| L2 | pnpm typecheck | PASS | tsc --noEmit 零输出，exit 0 |
| L3 | pnpm lint | PASS | eslint 零输出，exit 0 |
| L4 | pnpm build | PASS | ✓ Compiled successfully，路由表含新端点 ƒ /api/rooms/[room]/stats/reset |
| L5 | node tests/qa/commit-audit.mjs --branch main | PASS | total=195 pass=189 skip=6 fail=0 |
| L6 | 浏览器探针（下表 C6-C10） | PASS | 产线构建 hermetic 库 :3113 |

## 二、浏览器探针（产线构建，hermetic 库，:3113）

| # | 探针 | 判定 | 关键证据 |
| --- | --- | --- | --- |
| C6 | room-reset-qa.mjs（新） | PASS | N1 unknown room: POST reset → 404 problem+json + no silent create；S1 seed → /result non-zero ledger + reset entry visible；exit 0 |
| C7 | result-fresh-qa.mjs（新） | PASS | W-F1 win：auto-nav /result first frame totalGames=1（zero reload）；W-F2 draw：draws=1（zero reload）；exit 0 |
| C8 | one-identity-qa.mjs（回归） | PASS | 10/10 步（q1a/q1b/q1c-home-zero-api A1 面 + anonymous/offline 无 /api/* A4 面），exit 0 |
| C9 | result-celebration-qa.mjs（回归） | PASS | 5 PASS / 0 FAIL（F1 哨兵清除、F2 刷新不重放、F4 平局零哨兵——W-A 契约未回退），exit 0 |
| C10 | online-direct-qa.mjs（回归） | PASS | F7/F8 两步 PASS，exit 0 |

## 三、API 级对抗抽查（curl 直打 :3113）

| # | 检查 | 判定 | 关键证据 |
| --- | --- | --- | --- |
| C11 | 身份保留闭环 | PASS | ①POST /api/rooms→200 {existed:false} ②outcomes X→200 totalGames:1,xWins:1,currentStreak:1 ③reset→200 全零行（含 currentStreak:0，身份保留非销户）④再 outcomes X→200 totalGames:1——清零后记账闭环完好，无 404 |
| C12 | 未知房间 reset 负对照 | PASS | HTTP 404 + CONTENT_TYPE:application/problem+json，slug stats-not-found；追加 GET .../stats 仍 404 → 零静默建档 |
| C13 | 非法房间名 422 | PASS | %0A 控制字符→422 invalid-room-name；25 字符→422；%01 在 outcomes 端点交叉验证→422 同 slug |

## 四、登记在案的预期缺口（免扣分项，不影响判定）

1. Vercel/Turso 真实远端 HTTP 路径未测（本地 sqlite hermetic 覆盖）。
2. :3101 全量探针套件未重跑（仅覆盖受影响面 C6-C10）。
3. DATABASE_URL_SLOW_DELAY_MS 放大窗口未探（ordering 确定性由 L1 中 D-6/D-7 vitest 断言覆盖：W-F1~W-F4 ordering 四断言 + seam 三态；探针只保 E2E 不回归）。

## 五、调度者独立抽查补记

- 两执行席提交（7440a77 / e15e049）文件集与 plan §4 白名单逐文件比对：零越界；lore trailer 齐全（Constraint/Rejected/Confidence/Scope-risk/Directive/Tested|Not-tested + Plan footer）。
- 席 B 对 D-6 的实现细化（`hasPendingOutcomeWrite` 探测 + `trackOutcomeWrite` 登记，无在途写保持同步 push）在 D-6/D-7 拍板语义内，且保住「offline / anonymous 路径零行为变化」红线——接受。
- 合入后 dev 线性：5208302（plan）← e15e049（W-F）← 9fe08aa（W-R）。
