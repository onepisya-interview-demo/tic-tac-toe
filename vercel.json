{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "if git log -1 --pretty=%s | grep -qE '^docs[(:]'; then exit 0; elif git diff --name-only HEAD~1 HEAD | grep -qE '^(app|public|lib|components|db)/|package\\.json$|next\\.config'; then exit 1; else exit 0; fi"
}